note 20_count_characters.vss — Character Counter
make text becomes "Hello Very Simple Syntax 3.0"
make length becomes string.length(text)

say "Text: " + text
say "Total Length: " + length
say "Upper case: " + string.upper(text)
say "Lower case: " + string.lower(text)
