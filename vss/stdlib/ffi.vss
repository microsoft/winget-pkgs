note VSS Foreign Function Interface (Dynamic C Library Invocation)

task open needs lib_path
    send __ffi_open(lib_path)
finish

task call needs handle, symbol
    send __ffi_call(handle, symbol)
finish

task close needs handle
    send __ffi_close(handle)
finish
