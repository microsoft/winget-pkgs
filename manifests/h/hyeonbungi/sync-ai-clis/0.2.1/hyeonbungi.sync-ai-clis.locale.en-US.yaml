# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: hyeonbungi.sync-ai-clis
PackageVersion: 0.2.1
PackageLocale: en-US
Publisher: hyeonbungi
PublisherUrl: https://github.com/hyeonbungi
PublisherSupportUrl: https://github.com/hyeonbungi/sync-ai-clis/issues
PackageName: sync-ai-clis
PackageUrl: https://github.com/hyeonbungi/sync-ai-clis
License: MIT
LicenseUrl: https://github.com/hyeonbungi/sync-ai-clis/blob/HEAD/LICENSE
ShortDescription: Detect, install, and keep AI coding CLIs up to date with one command
Description: A cross-platform CLI that reconciles your machine toward "every known AI coding CLI installed, working, and current" - installed tools get updated through the channel they were installed with, missing tools are installed after consent, and each tool is re-verified after the work.
Moniker: sync-ai-clis
Tags:
- ai
- claude
- cli
- codex
- gemini
- installer
- updater
ReleaseNotes: |-
  Release Notes
  Fixed
  - npm updates now run through the npm that owns the existing install (resolved from the install's real location and invoked by absolute path). A bare npm install -g resolves to whichever npm is active — under nvm that meant installing a second copy into a different prefix instead of updating. --dry-run shows the exact invocation.
  - Homebrew formulas that vendor npm packages inside their Cellar (e.g. gemini-cli) are classified as brew installs again: Cellar/Caskroom paths now take precedence over the node_modules marker, so these tools update via brew upgrade instead of npm. npm globals installed through a brew-managed Node still classify as npm.
  Install sync-ai-clis 0.2.1
  Install prebuilt binaries via shell script
  curl --proto '=https' --tlsv1.2 -LsSf https://github.com/hyeonbungi/sync-ai-clis/releases/download/v0.2.1/sync-ai-clis-installer.sh | sh
  Install prebuilt binaries via powershell script
  powershell -ExecutionPolicy Bypass -c "irm https://github.com/hyeonbungi/sync-ai-clis/releases/download/v0.2.1/sync-ai-clis-installer.ps1 | iex"
  Install prebuilt binaries via Homebrew
  brew install hyeonbungi/tap/sync-ai-clis
  Install prebuilt binaries into your npm project
  npm install sync-ai-clis@0.2.1
  Download sync-ai-clis 0.2.1
  ──────────────────────────────────────────────────┬───────────────────┬─────────────
  File                                              │Platform           │Checksum
  ──────────────────────────────────────────────────┼───────────────────┼─────────────
  sync-ai-clis-aarch64-apple-darwin.tar.gz          │Apple Silicon macOS│checksum
  ──────────────────────────────────────────────────┼───────────────────┼─────────────
  sync-ai-clis-x86_64-apple-darwin.tar.gz           │Intel macOS        │checksum
  ──────────────────────────────────────────────────┼───────────────────┼─────────────
  sync-ai-clis-x86_64-pc-windows-msvc.zip           │x64 Windows        │checksum
  ──────────────────────────────────────────────────┼───────────────────┼─────────────
  sync-ai-clis-x86_64-pc-windows-msvc.msi           │x64 Windows        │checksum
  ──────────────────────────────────────────────────┼───────────────────┼─────────────
  sync-ai-clis-aarch64-unknown-linux-gnu.tar.gz     │ARM64 Linux        │checksum
  ──────────────────────────────────────────────────┼───────────────────┼─────────────
  sync-ai-clis-x86_64-unknown-linux-gnu.tar.gz      │x64 Linux          │checksum
  ──────────────────────────────────────────────────┴───────────────────┴─────────────
ReleaseNotesUrl: https://github.com/hyeonbungi/sync-ai-clis/releases/tag/v0.2.1
ManifestType: defaultLocale
ManifestVersion: 1.12.0
