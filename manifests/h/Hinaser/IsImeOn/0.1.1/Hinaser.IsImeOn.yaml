# winget 提出用マニフェスト(雛形)。実際の提出は wingetcreate を使うのが早い:
#   wingetcreate update Hinaser.IsImeOn -u <zipのURL> -v <version> -s
# 手書きで出す場合は winget-pkgs リポジトリの
#   manifests/h/Hinaser/IsImeOn/<version>/ に3ファイル(version/installer/locale)を置く。
# InstallerSha256 はリリースの SHA256SUMS.txt の値をそのまま使う。
PackageIdentifier: Hinaser.IsImeOn
PackageVersion: 0.1.1
DefaultLocale: ja-JP
ManifestType: version
ManifestVersion: 1.6.0
