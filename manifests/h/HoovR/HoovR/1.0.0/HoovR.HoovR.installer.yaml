# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.10.0.schema.json
PackageIdentifier: HoovR.HoovR
PackageVersion: "1.0.0"
InstallerType: zip
# `portable` is what puts `hoovr` on PATH without an installer: winget unpacks the zip and
# registers an app-execution alias. The nested type and file list are required once the
# artifact is an archive rather than a bare .exe.
NestedInstallerType: portable
NestedInstallerFiles:
  - RelativeFilePath: "hoovr-1.0.0-x86_64-pc-windows-msvc\\hoovr.exe"
    PortableCommandAlias: hoovr
ReleaseDate: "2026-09-25"
Installers:
  - Architecture: x64
    InstallerUrl: "https://github.com/RDalziel/hoovr-releases/releases/download/v1.0.0/hoovr-1.0.0-x86_64-pc-windows-msvc.zip"
    InstallerSha256: "55597A3B4130A6F254975CD8A94700500FEDC5DE9983A601BDDFE3ABE6CACF17"
ManifestType: installer
ManifestVersion: 1.10.0
