# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: laurentiu021.SysManager
PackageVersion: 1.52.89
PackageLocale: en-US
Publisher: laurentiu021
PublisherUrl: https://github.com/laurentiu021
PublisherSupportUrl: https://github.com/laurentiu021/SystemManager/issues
PackageName: SysManager
PackageUrl: https://github.com/laurentiu021/SystemManager
License: MIT
LicenseUrl: https://github.com/laurentiu021/SystemManager/blob/HEAD/LICENSE
ShortDescription: Modern Windows system toolkit — network monitor, performance tuning, cleanup, battery health, process manager, and more.
Description: |-
  SysManager is a modern Windows system toolkit built with WPF and .NET 10.
  It provides a comprehensive set of tools in a single application including
  live network monitoring, ping and traceroute, speed tests, performance tuning
  with power plan management, deep disk cleanup, duplicate file finder, disk
  analyzer, battery health monitoring, process manager, app uninstaller,
  startup control, Windows services manager, Windows features manager, driver
  information, and system health overview.
Tags:
- battery
- cleanup
- dotnet
- network-monitor
- open-source
- performance
- process-manager
- system-utility
- uninstaller
- windows
- wpf
ReleaseNotes: |-
  [1.52.89] - 2026-07-10
  Fixed
  - An environment variable added and applied, then deleted in the same session, could not actually be removed — it stayed orphaned in the registry while the pending-change counter got stuck. ApplyChanges maintains two parallel maps: _baseline (drives the pending-change count) and _originals (the sole source for the deletion list). The delete loop removed the variable from both maps, but the add/edit loop updated only _baseline — so a freshly-added variable was never recorded in _originals. If the user then deleted that variable and pressed Apply again, RecomputePending (reads _baseline) counted it as a pending deletion and the UI showed "1 pending change", but DeletedEntries (reads _originals) didn't include it, so it was never deleted from the registry — leaving PendingChangeCount stuck at 1 and the variable orphaned. (A manual Refresh silently self-healed it by reloading _originals.) The apply loop now writes _originals[Key(v)] alongside _baseline[Key(v)] on a successful SetVariable, keeping the two maps in lock-step.
  Verify the download
  Get-FileHash .\SysManager-v1.52.89.exe -Algorithm SHA256
  Expected SHA256: 9A8B6110F4700D07421D5547F43D5CDD402464C32A4FCBE63D0B1D995F1B9463
ReleaseNotesUrl: https://github.com/laurentiu021/SystemManager/releases/tag/v1.52.89
ManifestType: defaultLocale
ManifestVersion: 1.12.0
