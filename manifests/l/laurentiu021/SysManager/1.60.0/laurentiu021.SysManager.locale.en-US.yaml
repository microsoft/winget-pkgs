# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: laurentiu021.SysManager
PackageVersion: 1.60.0
PackageLocale: en-US
Publisher: laurentiu021
PublisherUrl: https://github.com/laurentiu021
PublisherSupportUrl: https://github.com/laurentiu021/SystemManager/issues
PackageName: SysManager
PackageUrl: https://github.com/laurentiu021/SystemManager
License: MIT
LicenseUrl: https://github.com/laurentiu021/SystemManager/blob/HEAD/LICENSE
ShortDescription: Modern Windows system toolkit — network monitor, performance tuning, cleanup, battery health, process manager, and more.
Description: |-
  SysManager is a modern Windows system toolkit built with WPF and .NET 10.
  It provides a comprehensive set of tools in a single application including
  live network monitoring, ping and traceroute, speed tests, performance tuning
  with power plan management, deep disk cleanup, duplicate file finder, disk
  analyzer, battery health monitoring, process manager, app uninstaller,
  startup control, Windows services manager, Windows features manager, driver
  information, and system health overview.
Tags:
- battery
- cleanup
- dotnet
- network-monitor
- open-source
- performance
- process-manager
- system-utility
- uninstaller
- windows
- wpf
ReleaseNotes: |-
  [1.60.0] - 2026-08-10
  Added
  - Duplicate Finder now tells you which copy to keep. It would find five identical photos, tell you they were wasting 4 GB, and then show them as five identical rows — leaving you to work out which one is the original, which is exactly the judgement the tab exists to help with. One file per group is now badged Keep: the oldest, because a copy is normally made after the file it came from. The rule is written on screen instead of applied quietly, every row shows its date so you can check the reasoning yourself, and "Keep this one" moves the badge when you know better — a copy that kept its original timestamp, or a file rewritten by cloud sync, will fool the guess. Groups with identical dates fall back to the copy nearest the top of the folder tree, so the badge never jumps around between scans.
  - Still nothing is deleted. The tab remains read-only on purpose: getting this wrong costs you your own photos and documents with no way back. It suggests a decision and points you at the file; the removing is yours to do, in Explorer.
  Verify the download
  Get-FileHash .\SysManager-v1.60.0.exe -Algorithm SHA256
  Expected SHA256: A09CCC535122F795AE372270FC017A0FE46BC4C82344D557AEBD299B4AC498B9
  This build is not code-signed, so Windows will warn on first launch. That is
  expected for a small independent app: see what the warning says and what to
  click.
  Verify where this binary came from
  This exe was built by GitHub Actions from this public source, and the build is
  recorded in the Sigstore public transparency log. With the
  GitHub CLI:
  gh attestation verify .\SysManager-v1.60.0.exe --repo laurentiu021/SystemManager
  That proves which commit and workflow produced this exact binary, something the
  SHA256 file cannot show, since it is published alongside the exe it describes.
  Each release also ships SysManager-v1.60.0.sbom.json, a CycloneDX inventory of
  every dependency resolved for this build.
ReleaseNotesUrl: https://github.com/laurentiu021/SystemManager/releases/tag/v1.60.0
ManifestType: defaultLocale
ManifestVersion: 1.12.0
