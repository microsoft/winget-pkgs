# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: laurentiu021.SysManager
PackageVersion: 1.52.90
PackageLocale: en-US
Publisher: laurentiu021
PublisherUrl: https://github.com/laurentiu021
PublisherSupportUrl: https://github.com/laurentiu021/SystemManager/issues
PackageName: SysManager
PackageUrl: https://github.com/laurentiu021/SystemManager
License: MIT
LicenseUrl: https://github.com/laurentiu021/SystemManager/blob/HEAD/LICENSE
ShortDescription: Modern Windows system toolkit — network monitor, performance tuning, cleanup, battery health, process manager, and more.
Description: |-
  SysManager is a modern Windows system toolkit built with WPF and .NET 10.
  It provides a comprehensive set of tools in a single application including
  live network monitoring, ping and traceroute, speed tests, performance tuning
  with power plan management, deep disk cleanup, duplicate file finder, disk
  analyzer, battery health monitoring, process manager, app uninstaller,
  startup control, Windows services manager, Windows features manager, driver
  information, and system health overview.
Tags:
- battery
- cleanup
- dotnet
- network-monitor
- open-source
- performance
- process-manager
- system-utility
- uninstaller
- windows
- wpf
ReleaseNotes: |-
  [1.52.90] - 2026-07-10
  Fixed
  - A single transient WMI hiccup on startup could permanently show "Unknown CPU" / "Windows" / no disks / no RAM modules for the whole session. SystemInfoService caches static hardware info with ??=, but every Query* helper returned a non-null fallback on a WMI fault (CpuInfo("Unknown", …), OsInfo("Windows", …), an empty disk/module list). Because the fallback was non-null, ??= stored it and never re-queried — so if the first CaptureAsync raced a transient WMI fault (plausible under the ~36-service startup load → RPC server too busy), the fallback defaults were cached process-wide (the service is a singleton) even after WMI recovered milliseconds later. The Dashboard, System Info tab, System Report and tray tooltip stayed stuck on the fallbacks until restart. Each Query* now returns null on a WMI fault (distinguished from a genuinely empty-but-successful result, which is still cached), so ??= caches only a successful query and retries on the next poll; Capture uses a transient, non-cached default for the current snapshot only.
  Verify the download
  Get-FileHash .\SysManager-v1.52.90.exe -Algorithm SHA256
  Expected SHA256: DA0D878A95D1D429E8BD2DF73ECC387D111091BB59CBB98DED8DDE55E27D077C
ReleaseNotesUrl: https://github.com/laurentiu021/SystemManager/releases/tag/v1.52.90
ManifestType: defaultLocale
ManifestVersion: 1.12.0
