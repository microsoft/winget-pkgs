# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: laurentiu021.SysManager
PackageVersion: 1.20.57
PackageLocale: en-US
Publisher: laurentiu021
PublisherUrl: https://github.com/laurentiu021
PublisherSupportUrl: https://github.com/laurentiu021/SystemManager/issues
PackageName: SysManager
PackageUrl: https://github.com/laurentiu021/SystemManager
License: MIT
LicenseUrl: https://github.com/laurentiu021/SystemManager/blob/HEAD/LICENSE
ShortDescription: Modern Windows system toolkit — network monitor, performance tuning, cleanup, battery health, process manager, and more.
Description: |-
  SysManager is a modern Windows system toolkit built with WPF and .NET 10.
  It provides a comprehensive set of tools in a single application including
  live network monitoring, ping and traceroute, speed tests, performance tuning
  with power plan management, deep disk cleanup, duplicate file finder, disk
  analyzer, battery health monitoring, process manager, app uninstaller,
  startup control, Windows services manager, Windows features manager, driver
  information, and system health overview.
Tags:
- battery
- cleanup
- dotnet
- network-monitor
- open-source
- performance
- process-manager
- system-utility
- uninstaller
- windows
- wpf
ReleaseNotes: |-
  [1.20.57] - 2026-06-24
  Security
  - The Uninstaller no longer runs a per-user app's uninstaller from a user-writable folder while elevated. When SysManager ran as Administrator, uninstalling a local app executed the path from its registry UninstallString (and any DLL a rundll32 command would load). That path was trusted even when it pointed inside %LocalAppData%, which a standard user can write to — and the uninstall key in HKCU is also user-writable. An unprivileged attacker could therefore plant a binary there plus a fake uninstall entry, then have an elevated SysManager run it with admin rights (local privilege escalation). Admin-protected locations (Program Files, Windows, ProgramData) stay trusted as before; the per-user location is now trusted only when SysManager is not elevated, so normal per-user uninstalls (e.g. VS Code, Discord) are unaffected.
  Verify the download
  Get-FileHash .\SysManager-v1.20.57.exe -Algorithm SHA256
  Expected SHA256: 1CFDA9E447A98344FF750B08D5625C8175CE90670B684FF35B6B3A89A688DA6A
ReleaseNotesUrl: https://github.com/laurentiu021/SystemManager/releases/tag/v1.20.57
ManifestType: defaultLocale
ManifestVersion: 1.12.0
