# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: laurentiu021.SysManager
PackageVersion: 1.52.49
PackageLocale: en-US
Publisher: laurentiu021
PublisherUrl: https://github.com/laurentiu021
PublisherSupportUrl: https://github.com/laurentiu021/SystemManager/issues
PackageName: SysManager
PackageUrl: https://github.com/laurentiu021/SystemManager
License: MIT
LicenseUrl: https://github.com/laurentiu021/SystemManager/blob/HEAD/LICENSE
ShortDescription: Modern Windows system toolkit — network monitor, performance tuning, cleanup, battery health, process manager, and more.
Description: |-
  SysManager is a modern Windows system toolkit built with WPF and .NET 10.
  It provides a comprehensive set of tools in a single application including
  live network monitoring, ping and traceroute, speed tests, performance tuning
  with power plan management, deep disk cleanup, duplicate file finder, disk
  analyzer, battery health monitoring, process manager, app uninstaller,
  startup control, Windows services manager, Windows features manager, driver
  information, and system health overview.
Tags:
- battery
- cleanup
- dotnet
- network-monitor
- open-source
- performance
- process-manager
- system-utility
- uninstaller
- windows
- wpf
ReleaseNotes: |-
  [1.52.49] - 2026-07-08
  Fixed
  - GPU temperature polling no longer re-initializes the NVIDIA API every couple of seconds — and no longer throws once per poll on PCs without an NVIDIA GPU. The temperature reader called NVIDIA's Initialize() and re-enumerated GPUs on every poll (about every 2 seconds). On a machine with no NVIDIA GPU that raised and swallowed an exception on every single poll; on NVIDIA machines it redid the one-time setup each time. SysManager now initializes the NVIDIA API at most once, remembers whether an NVIDIA GPU is present, and skips the read entirely afterwards when there isn't one — so there's no per-poll exception and no repeated setup. GPU temperatures on NVIDIA machines are unchanged.
  Verify the download
  Get-FileHash .\SysManager-v1.52.49.exe -Algorithm SHA256
  Expected SHA256: 8F641B259975DB4F53D683F664D5F307D538D75767AF8D122C7FA494C7F91671
ReleaseNotesUrl: https://github.com/laurentiu021/SystemManager/releases/tag/v1.52.49
ManifestType: defaultLocale
ManifestVersion: 1.12.0
