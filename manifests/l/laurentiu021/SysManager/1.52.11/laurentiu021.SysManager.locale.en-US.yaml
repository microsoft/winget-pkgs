# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: laurentiu021.SysManager
PackageVersion: 1.52.11
PackageLocale: en-US
Publisher: laurentiu021
PublisherUrl: https://github.com/laurentiu021
PublisherSupportUrl: https://github.com/laurentiu021/SystemManager/issues
PackageName: SysManager
PackageUrl: https://github.com/laurentiu021/SystemManager
License: MIT
LicenseUrl: https://github.com/laurentiu021/SystemManager/blob/HEAD/LICENSE
ShortDescription: Modern Windows system toolkit — network monitor, performance tuning, cleanup, battery health, process manager, and more.
Description: |-
  SysManager is a modern Windows system toolkit built with WPF and .NET 10.
  It provides a comprehensive set of tools in a single application including
  live network monitoring, ping and traceroute, speed tests, performance tuning
  with power plan management, deep disk cleanup, duplicate file finder, disk
  analyzer, battery health monitoring, process manager, app uninstaller,
  startup control, Windows services manager, Windows features manager, driver
  information, and system health overview.
Tags:
- battery
- cleanup
- dotnet
- network-monitor
- open-source
- performance
- process-manager
- system-utility
- uninstaller
- windows
- wpf
ReleaseNotes: |-
  [1.52.11] - 2026-07-03
  Fixed
  - Performance tab now reverts correctly on non-English Windows. Two power settings were read by matching English text that Windows translates in other display languages, so on a non-English system the "Restore" path misbehaved:
    - The active power plan was located by the English "GUID:" label. On a localized Windows that label is translated, so the plan couldn't be read and Restore silently skipped restoring it. The plan is now identified by its GUID (identical in every language), so it restores correctly regardless of display language.
    - The processor minimum state was read by an English label and fell back to a fabricated 5% when the label didn't match — which then got written back as the "restored" value on non-English machines. It now returns "unknown" when it genuinely can't be read, and Restore leaves the setting untouched rather than forcing a wrong value. The English fast-path is unchanged.
  Verify the download
  Get-FileHash .\SysManager-v1.52.11.exe -Algorithm SHA256
  Expected SHA256: CB86EBB227B21828FE400A27249BAD882740E484B9F3148B357754F804C3CBFA
ReleaseNotesUrl: https://github.com/laurentiu021/SystemManager/releases/tag/v1.52.11
ManifestType: defaultLocale
ManifestVersion: 1.12.0
