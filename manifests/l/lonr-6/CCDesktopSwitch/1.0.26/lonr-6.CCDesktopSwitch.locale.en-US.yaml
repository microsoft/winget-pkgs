# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: lonr-6.CCDesktopSwitch
PackageVersion: 1.0.26
PackageLocale: en-US
Publisher: CC Desktop Switch
PublisherUrl: https://github.com/lonr-6
PublisherSupportUrl: https://github.com/lonr-6/cc-desktop-switch/issues
PackageName: CC Desktop Switch
PackageUrl: https://github.com/lonr-6/cc-desktop-switch
License: MIT
LicenseUrl: https://github.com/lonr-6/cc-desktop-switch/blob/HEAD/LICENSE.txt
Copyright: Copyright (c) 2026 CC Desktop Switch
ShortDescription: Lightweight desktop tool for configuring DeepSeek, Kimi, Zhipu GLM and Bailian providers in Claude Desktop.
Description: |-
  CC Desktop Switch is a lightweight desktop app for the official Claude Desktop client. It lets you manage third-party Anthropic-compatible API providers such as DeepSeek, Kimi, Zhipu GLM, Alibaba Cloud Bailian, and Xiaomi MiMo, then apply the right Claude Desktop 3P configuration with one click.
  This project is focused on Claude Desktop on Windows and macOS. It is different from CLI-oriented tools such as farion1231/cc-switch: the goal here is to give regular desktop users a simple UI for provider setup, model mapping, health checks, and local gateway compatibility.
  Since v1.0.18, Claude Desktop is configured to call the local CC Desktop Switch gateway at 127.0.0.1. Keep CC Desktop Switch running in the background when using third-party providers. Closing the window keeps the app available in the tray on Windows or hidden in the Dock app lifecycle on macOS.

  What It Does
  - Manages DeepSeek, Kimi, Zhipu GLM, Alibaba Cloud Bailian, Xiaomi MiMo, and custom third-party providers.
  - Applies Claude Desktop third-party inference settings on Windows and macOS.
  - Uses a local gateway to keep model mapping, protocol compatibility, extra headers, and upstream keys under local control.
  - Shows only explicitly mapped Claude-safe model routes in Claude Desktop.
  - Rejects unmapped Claude model routes instead of silently falling back to an internal default.
  - Imports Anthropic-compatible CC-Switch configurations while leaving OpenAI-format entries opt-in.
  - Provides provider connectivity checks, model availability checks, SSE streaming, and custom upstream HTTP/SOCKS proxy support.
  - Prevents duplicate Windows app instances: launching the shortcut again brings the existing window forward.
ReleaseNotes: |-
  This patch release hardens provider editing, upstream diagnostics, uninstall cleanup, and the cross-platform release gate.
  - Fixed provider model mappings with legacy aliases being restored after the user explicitly removed Opus, Sonnet, or Haiku slots.
  - Tightened DeepSeek Max compatibility detection. Authentication, URL, model, context-length, and unrelated parameter errors now keep their original upstream diagnosis instead of being mislabeled as “Max is unsupported.”
  - Fixed lowercase and canonical OpenAI-compatible preset metadata so presets are routed through the existing API-format normalizer.
  - On Windows, a normal uninstall now removes only Claude Desktop policy values marked with ccds_managed=true. Silent in-place upgrades preserve the active provider configuration through /UPGRADE.
  - Fixed a Windows in-place upgrade race by running the old NSIS uninstaller synchronously before the replacement installer writes files and uninstall metadata.
  - Added Ubuntu, Windows, and macOS test coverage, frontend JavaScript syntax checks, and NSIS installer compilation.
  - Updated GitHub Actions to the current Node 24 action generations and documented the boundaries for Claude account features and GitHub Copilot BYOK.
  Upgrade notes
  No provider migration is required. After upgrading, re-apply the active provider to Claude Desktop and fully restart Claude Desktop. Users uninstalling v1.0.25 or earlier should use Clear Desktop configuration before removing the old version.
ReleaseNotesUrl: https://github.com/lonr-6/cc-desktop-switch/releases/tag/v1.0.24
ManifestType: defaultLocale
ManifestVersion: 1.12.0
