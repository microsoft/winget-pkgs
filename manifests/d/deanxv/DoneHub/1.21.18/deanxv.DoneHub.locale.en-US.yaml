# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: deanxv.DoneHub
PackageVersion: 1.21.18
PackageLocale: en-US
Publisher: Dean
PublisherUrl: https://github.com/deanxv
PublisherSupportUrl: https://github.com/deanxv/done-hub/issues
PackageName: Done Hub
PackageUrl: https://github.com/deanxv/done-hub
License: Apache-2.0
LicenseUrl: https://github.com/deanxv/done-hub/blob/HEAD/LICENSE
Copyright: Copyright (C) 2026 deanxv. All rights reserved.
ShortDescription: All in Done Hub service for OpenAI API.
Tags:
- ai
- chatbot
- large-language-model
- llm
- openai-api
ReleaseNotes: Yo update dropped, nobody asked but here we are—retries now let you set cooldown per status code like 503 gets two minutes in timeout jail, 502 gets one, 429 already knows what it did, logs finally tell the real reason you failed instead of just "lol retry_exhausted," graceful shutdown means Ctrl+C doesn't nuke your in-flight requests anymore, Gemini fixed so one dead key doesn't take the whole chain down with it, 504 and 524 actually get a second chance now (408 still dead sorry not sorry), Claude does byte passthrough so all your weird fields survive, image generation auto-downgrades when you accidentally call it via chat so your base64 doesn't turn into a million-token bill that costs more than your rent, connection pools not stuck in 2005 anymore, tokens got billing tags so finance stops crying, admins can transfer tokens across users like Robin Hood but legal, new "trusted employee" role that's better than a normie but worse than an admin, sidebar rewritten so it finally looks like 2026, quota input remembers if you like dollars or tokens, plus some boring Stripe and Redis stuff—anyway update if you want, I'm not your mom, bye.
ReleaseNotesUrl: https://github.com/deanxv/done-hub/releases/tag/v1.21.18
ManifestType: defaultLocale
ManifestVersion: 1.12.0
