# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: kts982.WinTUI
PackageVersion: 2.2.0
PackageLocale: en-US
Publisher: kts982
PublisherUrl: https://github.com/kts982
PublisherSupportUrl: https://github.com/kts982/wintui/issues
Author: Kostas T.
PackageName: WinTUI
PackageUrl: https://github.com/kts982/wintui
License: MIT
LicenseUrl: https://github.com/kts982/wintui/blob/main/LICENSE
ShortDescription: Terminal UI for winget on Windows.
Description: >-
  WinTUI is a terminal user interface for Windows Package Manager featuring a
  split-panel workspace for browsing, searching, installing, upgrading, and
  managing packages. Includes disk-persistent cache for instant startup,
  background refresh, integrated search with install queue, batch operations
  with a single UAC prompt, per-package rules and ignore filters, live
  command preview, system health checks, temp cleanup, and a headless CLI
  mode for scripting.
Moniker: wintui
Tags:
  - winget
  - windows
  - tui
  - terminal
  - package-manager
  - cli
ReleaseNotes: |-
  - Added per-package rules editor (`p` in the detail view) with scope, architecture, and elevate overrides, saved atomically under source-qualified keys.
  - Added ignore toggle (`i` in the detail view) that hides a package or a specific version from the Updates list; the Updates header shows a `(N hidden)` count and stale version-ignores expire automatically.
  - Added live command preview in the detail view showing the exact winget command that would run with active overrides applied.
  - Added `?` in the batch confirm modal to expand the same per-item command preview for every staged package.
  - Made the batch modal body scrollable with `↑↓`, `PgUp/PgDn`, `Home/End` so large batches no longer push the actions line or bottom border off-screen.
ReleaseNotesUrl: https://github.com/kts982/wintui/releases/tag/v2.2.0
ManifestType: defaultLocale
ManifestVersion: 1.12.0
