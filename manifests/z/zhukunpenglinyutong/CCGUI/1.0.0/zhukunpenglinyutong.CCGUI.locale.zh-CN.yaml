# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: zhukunpenglinyutong.CCGUI
PackageVersion: 1.0.0
PackageLocale: zh-CN
Publisher: zhukunpenglinyutong
PublisherUrl: https://github.com/zhukunpenglinyutong
PublisherSupportUrl: https://github.com/zhukunpenglinyutong/desktop-cc-gui/issues
Author: 朱昆鹏
PackageName: ccgui
PackageUrl: https://github.com/zhukunpenglinyutong/desktop-cc-gui
License: 免费软件
# LicenseUrl: https://github.com/zhukunpenglinyutong/desktop-cc-gui/blob/HEAD/LICENSE
Copyright: |-
  Copyright (c) 2026 Thomas Ricouard
  Copyright (c) 2026 zhukunpenglinyutong（朱昆鹏）
ShortDescription: CC GUI 客户端（专为开发者打造的 Vibe Coding 平台）
Description: CC GUI 目标群体是专业开发者，可以替代 Curosr 使用。专注于开发者体验，我们最终目标是打造一个 100% 开源透明的下一代 Vibe Coding 编辑器（支持 Claude Code，Codex 等引擎）
Tags:
- claude-code
- 人工智能
- 代码
- 大语言模型
- 编程
ReleaseNotes: |-
  New Features
  - 新增设置项，可选思考结束后保持思考过程展开
  - a queued message can be sent now
  - the axis answers with the range total
  - add Antigravity CLI as a first-class engine
  - add plugin marketplace with index fetch, remote install, and update checks
  - SDK 0.3.2 adds ctx.composer.setDraft with composer:draft grant
  - tab menu gains "close inactive"
  - 单位阶梯补到 T，万亿不再渲染成「1503.9B」
  - 首次点「外网访问」先弹风险提示，接受后才展开
  - 有外网设备连进来时在设置右侧浮出「远程控制中」徽标
  - 授权列表支持给设备改名，卡片标题改为「授权列表」
  - 配对页去掉无用的配对码；密钥框左侧加对称的更换密钥按钮
  - 设备配对改为桌面端确认授权，密钥不再直接放行
  - 适配 0.1.2+ 协议换代并新增 host 会话流式引擎
  - 外网访问一键部署中转 + 导出源码，中转状态点常显
  - broader slash-skill roots, proxy toggle tooltip, hover-open engine flyout
  - collapsible user messages with footer copy button
  - show ahead/behind counts vs upstream in changes panel
  - 外网访问重排为三卡并新增「复制源码」
  - 顶栏菜单支持添加自定义程序并提取图标
  - 远程访问 page split into 内网访问 / 外网访问
  - pairing-key authorization and an outbound relay
  - one-click proxy toggle in the composer footer
  - the ledger counts engine-reported requests, not prompts
  - real request counts, clearer totals, icon proxy toggle
  - usage tooltip follows the pointer and stays in the pane
  - vendor AI vendor marks; usage page loses its header row
  - vendor brand marks for the model vendors we serve
  - usage page polish — icon actions and per-CLI details
  - token usage page with a per-model daily chart
  - show live token usage on the running-turn strip
  - keep model sources visible in the picker while filtering
  - 官方配置在线编辑对话框 + InfoTip 组件抽取
  - CLI 一键安装/更新确认对话框与流式进度
  - CLI 管理侧边栏支持拖拽排序并持久化顺序
  - persist workspace expansion and add a copy button to user turns
  - render math with KaTeX
  - render math with KaTeX
  - CLI 生命周期管理与聊天斜杠命令支持
  - 支持上下文压缩与用量刷新、子代理运行状态展示及响应元数据增强
  - 退出前二次确认，防止误关窗口终止所有会话
  - 文件树 Git 状态 — 嵌套仓库徽标、状态颜色、根行显示（拆分结构适配版）
  - auto-fold thinking when it settles, expand shows full text
  - line-boundary thinking window with top fade instead of char-by-char wipe
  - 支持工具调用参数与结果展开、Git Diff/Bash 美化及完成元数据展示
  - grant extra directories on headless permission denial
  - per-tab model/effort overrides for composer picker
  - RunStatusStrip 替换 agent-progress，引擎事件透传 todo 快照
  - add Codex Fast toggle with stable compact controls
  - 插件系统（SDK + 运行时 + 管理界面）、命令面板与设置页重构
  - 代理设置、grant 库与签名构建脚本，聊天面板与历史功能增强
  - resolve Claude launch model from picker alias; dismissible error banners
  - add branch search filter to changes panel branch dropdown
  - DSH host、Agent 任务面板与 release 发布流水线
  - Claude 模型别名解析内嵌 registry，渠道 env 键剥离与模型分组列表
  - Windows CLI 二进制解析与进程树终止，修复停止竞态与 Claude 历史目录
  - 文件树/链接右键菜单、滚动跟随按钮、工作区设置与引擎模型层增强
  - 百度统计 Linux 原生传输、渠道模型列表拉取与 Provider 预设
  - 渠道改为写入各 CLI 原生配置文件，新增版本记录对话框
  - AI 聊天输入区权限/effort 档位、CLI 配置与终端视图增强
  - Pi 家族引擎认证、CC 切换与 AI 聊天输入区增强（提及/权限/分支菜单/提示历史）
  - 消息锚点导航、Provider 配置对话框与引擎/历史层增强
  - 新增局域网网页访问（WebSocket 桥接 + 设置页二维码入口）
  - 接入 OMP 引擎（pi-family 参数化复用 + 全链路接线 + OpenSpec add-omp-engine）
  - 增加首页外观设置与交互粒子字标
  Fixes
  - macOS DMG 打包失败自动重试
  - 移除 tauri signer sign 多余的 -- 分隔符
  - 兼容扫描 v0.9 托管渠道与老 kimi 目录的会话
  - resolve unused param warning and simplify state arg
  - hide DSH subagent sessions from the sidebar
  - settle codex turns that beat the send response, pipe pi prompts via stdin
  - one owner for a session's reasoning level
  - the session remembers the reasoning level it ran
  - a queued message goes out when the turn dies
  - keep agy transcripts to visible replies
  - the relay card follows a switch-only event
  - hide console window for plugin exec/spawn on Windows
  - an error banner stays a banner
  - the subagent panel reads the whole session, not just the last page
  - the switch and its target commit as one state
  - the switch stays on until the user turns it off
  - the phone layout stops jumping and keeps its top bar
  - the subagent panel reports what the harness reports
  - a failed dial retries instead of ending the session
  - let CLI settings choose the Codex home directory
  - the subagent pill counts subagents, not delegation calls
  - "close inactive" keeps the sessions that are still running
  - a session keeps the provider and model it ran
  - a turn another client started reads as running here too
  - 单条消息的 ↑/↓ 词元读数过千后进 M/B
  - 授权开关不再挡住已授权设备；部署密钥恒为新铸
  - 台账过十亿后进 B，两份 formatTokens 合成一份
  - 配对密钥真正一次性，中继不再被手机牵着走
  - WS 帧类型在中继链路上丢失，外网 web ui 因此空白
  - 外网 WS 被 400 打掉导致 web ui 空白；顺带收紧 via 头信任
  - 一键部署支持账号令牌（cfat_），并修通真实部署链路
  - 拨号失败即结束会话，开关自动回到「连接中转」
  - 中继报错移入状态点 tooltip，不再撑高卡片
  - resolve remote credentials via config helpers and ssh-agent for push/pull
  - codex tails its rollout from the end, not from byte 0
  - satisfy react-doctor export-components and html-sink rules
  - 修复 Windows 盘符路径带前导斜杠或行号后缀时文件链接无法解析打开
  - bound runaway resources across engine, plugin, and chat lifecycles
  - LaTeX [...] and (...) render instead of leaking as text
  - codex cache is part of its input, not extra
  - the key hint names both rotation triggers
  - connecting the relay turns the local bridge on
  - the auth card no longer hides with the LAN bridge
  - nested math-mode dollars render instead of leaking raw TeX
  - codex live usage from its session log, not the done line
  - Windows 下切换主题时同步原生窗口主题，修复深色模式状态栏颜色
  - the key rules read the same on every surface
  - the relay pane stops flashing a status line
  - the pairing key refreshes on every surface
  - count requests as they report, and the reply's running total
  - one row per model, without the relay provider
  - proxy toggle mirrors the context-meter button
  - 支持解析 Claude 压缩边界事件以准确统计上下文用量
  - model list loading hint, CLI-stacked chart, model badges
  - scale usage bars to the selected range
  - model list switches on click only, and single-provider results keep their header
  - 修复上下文窗口统计不准与缓存 Token 重复计算问题
  - hide Codex subagent rollouts from the sidebar
  - 修复 Codex 环境变量与 Pi 自定义模型目录
  - scope the model picker to the session
  - retries no longer kill the turn at 1/5
  - retries no longer kill the turn at 1/5
  - scope the model picker to the session
  - align context meter, 6-level effort, and Fast mode
  - keep the UI thread off the process-tree wait
  - stop button now actually kills the engine process tree
  - 审查修复 — 恢复 purple token、刷新链路补颜色、回退 CI/gitignore、变更后刷新树、颜色查找优化
  - registry rekey copies instead of moves so run-id interrupts still reach the child
  - surface nested turn_end errors (401), suppress Done after Error
  - kill engine process on terminal error so UI settle matches process state
  - merge interleaved thinking/text stream channels into single rows
  - declare directly imported codemirror and unified deps
  - percent-decode URL-encoded paths in chat file links
  - Windows-only test failures in history scanner and kimi paths
  - aggregate session kill, surface engine stderr, extract nested omp errors
  - 工具结果按 tool_use_id 归位，移除硬编码文案
  - Windows .cmd shim 下多行提示词只送达第一行
  - 调整 DSH host 设置区块与插件运行时 context
  - sync Codex task names from session index
  - strip chained context and migrate stale titles
  - hide console windows for spawned child processes
  - 移出工作区菜单项上移至设置别名下方
  - 修复右侧工具栏空白区域拖拽
  Performance Improvements
  - reduce delivery latency and isolate text scheduling
  - adapt reveal pacing to measured OMP output bursts
  - reveal streamed text per frame without reparsing markdown
  - cache completed code highlighting during streaming
  - cache anchor previews and memoize settled tool rows
  - skip stream snapshots and defer live syntax highlighting
  - dedupe dist embed and switch release to opt-level z
ReleaseNotesUrl: https://github.com/zhukunpenglinyutong/desktop-cc-gui/releases/tag/v1.0.0
ManifestType: defaultLocale
ManifestVersion: 1.12.0
