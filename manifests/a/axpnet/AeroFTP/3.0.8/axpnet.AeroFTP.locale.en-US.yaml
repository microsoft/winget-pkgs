# Created with WinGet Releaser using komac v2.15.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: axpnet.AeroFTP
PackageVersion: 3.0.8
PackageLocale: en-US
Publisher: axpnet
PublisherUrl: https://github.com/axpnet
PublisherSupportUrl: https://github.com/axpnet/aeroftp/issues
Author: axpnet
PackageName: AeroFTP
PackageUrl: https://github.com/axpnet/aeroftp
License: GPL-3.0
LicenseUrl: https://github.com/axpnet/aeroftp/blob/HEAD/LICENSE
ShortDescription: A modern, multi-protocol file manager and FTP client with integrated AI, cloud sync, and military-grade encryption.
Description: |-
  AeroFTP is an open-source, multi-protocol file manager built with Tauri 2 (Rust + React 18 + TypeScript).
  It supports 16 protocols (FTP, FTPS, SFTP, WebDAV, S3, Google Drive, Dropbox, OneDrive, MEGA, Box, pCloud, Azure Blob, 4shared, Filen, Zoho WorkDrive, and Cryptomator vaults),
  features an AI assistant with 45 tools and 15 LLM providers, military-grade encrypted vaults (AES-256-GCM-SIV + Argon2id),
  bidirectional real-time sync, a built-in code editor, SSH terminal, and media player with visualizers.
  Available in 47 languages with 4 themes.
Moniker: aeroftp
Tags:
- cloud-storage
- encryption
- file-manager
- file-transfer
- ftp
- ftp-client
- multi-protocol
- rust
- s3
- sftp
- sync
- tauri
- webdav
ReleaseNotes: |-
  [3.0.8] - 2026-03-21
  CLI Feature Parity & rclone-class Commands
  5 new CLI commands (head, tail, hashsum, touch, check), advanced filter system, sync safety features, and --profile UX overhaul. 12 providers tested live via CLI.
  Added
  - head / tail commands: Print first/last N lines of remote files. Default 20, configurable with -n. Binary file detection with exit code 5
  - hashsum command: Compute remote file hash — md5, sha1, sha256, sha512, blake3. Output matches sha256sum format. JSON mode with algorithm, hash, size
  - touch command: Create empty remote file or confirm existence. Returns action: "created" or action: "exists" in JSON mode
  - check command: Verify local/remote directory match — reports matches, differences, missing files. Supports --checksum (SHA-256) and --one-way modes
  - Advanced filter system: --include, --exclude-global (glob patterns), --include-from, --exclude-from (file-based), --min-size, --max-size, --min-age, --max-age. Applies to ls, get, put, sync, find
  - --max-delete sync safety: Abort if deletion count exceeds limit (absolute 50 or percentage 25%). Prevents accidental mass deletion
  - --backup-dir / --backup-suffix: Sync flags for backing up overwritten files (struct ready)
  Fixed
  - --profile without URL placeholder: All 19 CLI commands now work with just --profile "name" — no _ dummy URL needed. Positional arg shift handles profile mode transparently
  - connect command with --profile: Was bypassing vault profile resolution, going direct to URL parser. Now uses create_and_connect with full OAuth/profile support
  - 4shared CLI support: OAuth 1.0 tokens now correctly read from vault (oauth_fourshared key with token:secret format + individual consumer key/secret)
  - Azure container from profile: options.bucket auto-mapped to container for Azure provider — no manual --container flag needed when using saved profiles
  Changed
  - CLI from 13 to 18 commands: connect, ls, get, put, mkdir, rm, mv, cat, head, tail, touch, hashsum, check, stat, find, df, tree, sync
  - CLI documentation: Complete rewrite of CLI-GUIDE.md and docs.aeroftp.app with new commands, filters, test results
  - 12 providers tested live: SFTP, FTP, GitHub, MEGA, OpenDrive, Filen, Koofr (WebDAV + Native), WebDAV, S3, Azure, 4shared
  Downloads:
  - Windows: .msi installer or .exe
  - macOS: .dmg disk image
  - Linux: .deb, .rpm, .snap, or .AppImage
ReleaseNotesUrl: https://github.com/axpnet/aeroftp/releases/tag/v3.0.8
ManifestType: defaultLocale
ManifestVersion: 1.12.0
