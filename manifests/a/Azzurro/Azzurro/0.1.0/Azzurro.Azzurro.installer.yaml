# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.12.0.schema.json

# Generated by komac and kept in sync with what was actually submitted, so the
# next release starts from the real thing rather than from a draft.
#
# InstallerType portable because the exe has no installer: winget copies it and
# registers an alias. That makes Commands load-bearing — winget's alias order is
# Commands[0], --rename, PortableCommandAlias, then the file's own name, so
# without it the command would be "azzurro-v0.1.0-x86_64" and would change every
# release. Portable also means no Start Menu entry.
PackageIdentifier: Azzurro.Azzurro
PackageVersion: 0.1.0
InstallerType: portable
UpgradeBehavior: install
Commands:
- azzurro
ReleaseDate: 2026-09-01
Installers:
- Architecture: x64
  InstallerUrl: https://github.com/jzbz/azzurro/releases/download/v0.1.0/azzurro-v0.1.0-x86_64.exe
  # Taken from the release's PGP-signed SHA256SUMS rather than computed here,
  # so the hash winget pins is the one covered by that signature.
  InstallerSha256: 47A1F37CD85BF90A54C5AD86F950919C09A1541F39EB404EC564C9C197A0C1D3
ManifestType: installer
ManifestVersion: 1.12.0