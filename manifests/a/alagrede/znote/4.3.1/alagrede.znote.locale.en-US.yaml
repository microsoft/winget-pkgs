# Created with WinGet Updater using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: alagrede.znote
PackageVersion: 4.3.1
PackageLocale: en-US
Publisher: alagrede
PublisherUrl: https://github.com/alagrede
PublisherSupportUrl: https://github.com/alagrede/znote-app/issues
PackageName: znote
PackageUrl: https://github.com/alagrede/znote-app
License: Freeware
ShortDescription: Markdown-based note-taking app for developers
Tags:
- dev
- editor
- electron
- markdown
- react
- znote
ReleaseNotes: |-
  🔧 v4.3.1 — /rewrite reliability fixes
  Some models replied "Hit Rewrite note!"… to the button you just pressed —
  and that chatty reply could even replace your note. Fixed:
  - The final generation now runs under a dedicated rewrite-engine prompt
    (no more interview persona fighting the instruction), with one automatic
    retry if the model still doesn't comply.
  - A strict output contract: anything that isn't a real rewrite never
    touches your note — the reply just lands in the chat and the session
    continues.
  Rewrites stay atomic (one undo restores everything) and your frontmatter is
  always preserved. Custom replace: true commands benefit automatically.
ReleaseNotesUrl: https://github.com/alagrede/znote-app/releases/tag/v4.3.1
Documentations:
- DocumentLabel: FAQ
  DocumentUrl: https://znote.io/faq.html
ManifestType: defaultLocale
ManifestVersion: 1.12.0
