# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: izantech.yup
PackageVersion: v0.1.5
PackageLocale: en-US
Publisher: izantech
PublisherUrl: https://github.com/izantech
PublisherSupportUrl: https://github.com/izantech/yup/issues
PackageName: yup
PackageUrl: https://github.com/izantech/yup
License: MIT
LicenseUrl: https://github.com/izantech/yup/blob/HEAD/LICENSE
ShortDescription: Safe, cross-platform updater for development tools
Description: |-
  yup detects how each tool was installed (Homebrew, asdf, nvm, pyenv, rbenv, conda, etc.)
  and updates using the correct manager. On first run, it shows an interactive configuration
  wizard to select which managers to update. Subsequent runs execute directly with saved preferences.
Tags:
- cli
- developer-tools
- package-manager
- rust
- updater
ReleaseNotes: |-
  Release Notes
  Added
  - --greedy CLI flag to pass --greedy to brew upgrade commands, including auto-updating casks in the upgrade
  - Brew greedy mode can also be set as a default via [brew] section in config (greedy = true)
  - Configurable --yes flag for mise self-update via [mise] section in config (yes = true by default)
  - Corepack detection for pnpm: if corepack is available, uses corepack install -g pnpm@latest instead of pnpm self-update
  - Config wizard now prompts for brew --greedy and mise --yes options when those managers are selected
  Fixed
  - yup config now loads existing saved config and pre-selects previously enabled managers instead of selecting all by default
  Changed
  - mise self-update now passes --yes by default to skip the confirmation prompt (configurable via [mise] config section)
  Download yup 0.1.5
  ─────────────────────────────────────────┬───────────────────┬─────────────
  File                                     │Platform           │Checksum
  ─────────────────────────────────────────┼───────────────────┼─────────────
  yup-aarch64-apple-darwin.tar.xz          │Apple Silicon macOS│checksum
  ─────────────────────────────────────────┼───────────────────┼─────────────
  yup-x86_64-apple-darwin.tar.xz           │Intel macOS        │checksum
  ─────────────────────────────────────────┼───────────────────┼─────────────
  yup-aarch64-pc-windows-msvc.zip          │ARM64 Windows      │checksum
  ─────────────────────────────────────────┼───────────────────┼─────────────
  yup-x86_64-pc-windows-msvc.zip           │x64 Windows        │checksum
  ─────────────────────────────────────────┼───────────────────┼─────────────
  yup-aarch64-unknown-linux-gnu.tar.xz     │ARM64 Linux        │checksum
  ─────────────────────────────────────────┼───────────────────┼─────────────
  yup-x86_64-unknown-linux-gnu.tar.xz      │x64 Linux          │checksum
  ─────────────────────────────────────────┴───────────────────┴─────────────
ReleaseNotesUrl: https://github.com/izantech/yup/releases/tag/v0.1.5
ManifestType: defaultLocale
ManifestVersion: 1.12.0
