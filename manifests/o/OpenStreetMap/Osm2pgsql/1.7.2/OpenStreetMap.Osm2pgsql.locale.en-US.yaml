# Created with YamlCreate.ps1 v2.2.1 $debug=NVS0.CRLF.7-3-2.Win32NT
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.2.0.schema.json

PackageIdentifier: OpenStreetMap.Osm2pgsql
PackageVersion: 1.7.2
PackageLocale: en-US
Publisher: Osm2pgsql project
# PublisherUrl: 
PublisherSupportUrl: https://osm2pgsql.org/support/
# PrivacyUrl:
# Author:
PackageName: Osm2pgsql
PackageUrl: https://osm2pgsql.org/
License: GPL-2.0
# LicenseUrl:
# Copyright:
# CopyrightUrl:
ShortDescription: Osm2pgsql imports OpenStreetMap (OSM) data into a PostgreSQL/PostGIS database.
# Description:
# Moniker:
Tags:
- openstreetmap
- postgis
# Agreements:
ReleaseNotes: |- 
  The new release 1.7.2 has some small changes only:
  • The flex output now allows tables with only the id column (or columns).
  • The osm2pgsql-replication script now always expects the osm2pgsql binary to be in same path as itself.
  • Adds the flag --middle-schema=SCHEMA to the osm2pgsql-replication script which allows placing the replication status table in a schema other than PUBLIC (Thanks to @JakobMiksch).
  • More tests have been converted to the new BDD format.
  • Various code cleanups and refactorings especially in the expire code.
ReleaseNotesUrl: https://osm2pgsql.org/news/2022/11/10/release-1.7.2.html
# PurchaseUrl:
# InstallationNotes:
Documentations:
- DocumentLabel: Manual
  DocumentUrl: https://osm2pgsql.org/doc/manual.html
- DocumentLabel: FAQ
  DocumentUrl: https://osm2pgsql.org/doc/faq.html
ManifestType: defaultLocale
ManifestVersion: 1.4.0
