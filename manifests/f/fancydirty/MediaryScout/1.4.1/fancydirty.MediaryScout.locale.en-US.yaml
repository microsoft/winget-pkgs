# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.locale.1.12.0.schema.json

PackageIdentifier: fancydirty.MediaryScout
PackageVersion: 1.4.1
PackageLocale: en-US
ShortDescription: An agent-driven media library for your cloud drives.
Description: You ask for a movie, show, or anime; an LLM agent scouts resources across your indexers, transfers the best match into your own 115 / Quark / 光鸭 drive, verifies what landed, and keeps tracking what's still missing. The desktop app bundles its own SQLite data layer and runs the full engine inside an Electron shell — no Docker, no Postgres, no terminal.
Tags:
- agent
- ai
- cloud-drive
- drive
- large-language-model
- llm
- media-library
ManifestType: locale
ManifestVersion: 1.12.0
