# Created with komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: farion1231.CC-Switch
PackageVersion: 3.19.2
PackageLocale: en-US
Publisher: farion1231
PublisherUrl: https://github.com/farion1231
PublisherSupportUrl: https://github.com/farion1231/cc-switch/issues
Author: farion1231
PackageName: CC Switch
PackageUrl: https://github.com/farion1231/cc-switch
License: MIT
LicenseUrl: https://github.com/farion1231/cc-switch/blob/HEAD/LICENSE
ShortDescription: A cross-platform desktop All-in-One assistant tool for Claude Code, Codex, OpenCode, openclaw & Gemini CLI.
Moniker: ccswitch
Tags:
- ai-tools
- claude-code
- codex
- desktop-app
- mcp
- minimax
- omo
- open-source
- opencode
- provider-management
- rust
- skills
- skills-management
- tauri
- typescript
- wsl-support
ReleaseNotes: |-
  CC Switch v3.19.2
  > 这一版的主线是把数字算对、把边界扎紧：Codex 用量统计修掉一个会把交错计数器文件多算数倍的缺陷——升级后新数据即刻正确，历史虚高可用一次手动重建回正（见「升级提醒」）；一轮六项安全加固把用量脚本、会话日志、目录文件与代理缓冲响应体的无界读取全部封顶。日常顺手的部分也补齐了：MCP / 提示词 / Skills 面板可以搜索，MCP 与 Skills 还能按应用一键批量开关；认证中心逐账号显示 ChatGPT 订阅用量；OMO 用户拿回可用的集成。两条写盘路径批量化之后，大数据库的备份导入、自动同步与用量重导入不再动辄卡住。本版没有数据库迁移。
  English → | 日本語版 →
  重点内容：你现在可以
  - 拿到正确的 Codex 用量数字：真实日志里存在计数器交错的文件——同一份快照被网关换着限额桶反复重播，或两个累计计数器交替出现——旧算法会把它们当成新增量，现场文件实测有 6 到 8 倍虚高。修复经近 1,900 份真实会话文件回放验证，与独立重算的理想值偏差 0.001%。历史数据不会被自动改写，回正方法见升级提醒。
  - 在 MCP、提示词、Skills 面板里直接搜索：三个面板都有了搜索框；MCP 与 Skills 列表上的应用徽章还变成了三态开关，一键把某个应用在整张列表上批量启用或停用（#5954、#5935 都是用户之声）。
  - 让新版 Claude Code 直接进入 Copilot 接管的会话：新版 Claude Code 对不认识的 API key 会弹确认框、且默认选中「No (recommended)」，此前接管写入的占位符正好撞上它，用户看到的是一个未登录的会话。现在改写 ANTHROPIC_AUTH_TOKEN 占位符，零弹窗直接进入。
  - 继续用 CC Switch 管理 OMO：OMO 4.19.3 起配置统一进 ~/.omo/omo.jsonc，旧文件不再被读取，此前的供应商切换看似成功、实则全部落空。现在检测到统一配置文件（omo.jsonc 或 omo.json）就写进它的 "[opencode]" 分区。OMO 表单的模型选择器还会合并 opencode models 实际报告的运行时模型。
  - 在认证中心看到每个 ChatGPT 账号的订阅用量：多账号场景下不再需要逐个切换查看。
  - 在大数据库上不再动辄卡住：备份导出改为批量 INSERT、同步恢复改为单事务——此前每行一次 fsync 正是自动同步周期性卡顿的元凶；Codex 用量全量重导入在真实语料上从 36.3 秒降到 11.1 秒（macOS），Windows 收益更大。
  - 看到被网关吞掉的工具调用，而不是被静默晾在原地：第三方 Chat 网关返回缺函数名的工具调用时，转换层此前把它们丢掉还报「本轮完成」，Codex 于是无声地结束了循环。现在这种情况会明确报错，且丢弃点带结构化日志，#4341 这类问题终于可以从真实流量里诊断。
  - 让 Hermes 提示词真正生效：写入 Hermes 实际加载的 ~/.hermes/SOUL.md，而不是它从不读取的 AGENTS.md。
  - 装上此前装不了的 Skill：ast-grep 这类带同名空壳目录的仓库能装了；skills.sh 嵌套 Skill 的 README 链接不再 404（存量记录需重装一次恢复，见升级提醒）。
  使用攻略
  本版的改动集中在用量统计与扩展管理面板上，建议结合以下文档了解：
  - 用量统计：用量看板的数据来源与统计口径，理解交错计数器多算是怎么发生的、什么情况下需要手动重建。
  - MCP 管理 与 Skills 管理：新的搜索与批量开关所在的两个面板。
  Warning
  唯一官方渠道声明（请务必阅读）
  CC Switch 是完全免费、开源的桌面应用，不会向用户收取任何费用。请仅通过下列官方渠道获取本软件：
  ────────┬────────────────────────────────────
  类别    │唯一官方
  ────────┼────────────────────────────────────
  官网    │ccswitch.io
  ────────┼────────────────────────────────────
  源码    │github.com/farion1231/cc-switch
  ────────┼────────────────────────────────────
  下载    │GitHub Releases
  ────────┼────────────────────────────────────
  作者    │@farion1231
  ────────┼────────────────────────────────────
  举报山寨│GitHub Issues
  ────────┴────────────────────────────────────
  任何向你收费、要求充值、或索取登录凭据的"CC Switch"网站或客户端均为假冒。如果你被诱导支付了费用，请立即停止操作并通过 GitHub Issues 反馈。
  概览
  CC Switch v3.19.2 是一次以正确性为主的维护版本，主线有三条。第一条是把数字算对：Codex 会话导入器在计数器交错的文件上会把用量多算数倍（#3011），本版换用「优先取每轮增量、按完整签名识别重播」的算法，并以近 1,900 份真实会话文件的回放验证收口；备份与用量导入两条写盘路径同时批量化，大数据库导入卡顿显著缓解（#2100 或将受益）。
  第二条是把边界扎紧：一轮外部贡献的安全加固把用量脚本运行时、Grok 会话日志、Codex 目录文件与代理缓冲响应体的无界读取全部封顶，深链导入确认框补上两个此前收集却不显示的凭据字段。第三条是把日常顺手的东西补齐：MCP / 提示词 / Skills 三个面板的搜索、MCP 与 Skills 的批量应用开关、认证中心的逐账号订阅用量，以及 OMO 的统一配置与运行时模型两项适配。此外还有 Copilot 登录、Hermes 提示词、Skill 安装等七个具体故障修复。本版没有数据库 schema 迁移（版本号保持 v16），升级轻量。
  发布日期：2026-08-06
  更新规模：24 commits | 109 files changed | +12,340 / -1,897 lines
  新功能
  管理面板：搜索与批量应用开关
  MCP、提示词、Skills 三个面板都有了共享的搜索框——按 Esc 清空，且只在有内容时拦截全局返回快捷键，不影响你平时的操作习惯。MCP 与 Skills 列表顶部的应用计数徽章变成了三态开关按钮：一键把某个应用在整张列表上批量启用或停用，逐条点开关的日子结束了（#5954、#5935）。
  批量操作刻意串行执行而不是并发——每个应用的 live 配置是单个文件，并发写会互相覆盖；失败的条目会被收集起来一次性报告。批量开关作用于整张列表而不是搜索过滤后的子集，避免「以为只开了过滤结果、实际状态不明」的歧义。
  底层还顺手修掉两个数据层的老问题：MCP 开关从「读整行、改内存、写整行」改为单列原子 UPDATE，两个应用几乎同时切换时不再互相丢失更新；Skill 更新落库前会重新确认记录仍然存在、安装代数未变，慢速更新任务不再可能复活一个刚被卸载的 Skill。搜索索引是显式白名单——环境变量与请求头永远不进可搜索文本。
  认证中心：逐账号订阅用量
  设置 → 认证中心现在给每个 ChatGPT（Codex OAuth）账号内联显示订阅用量，复用供应商卡片页脚已有的查询——按账号去重、缓存五分钟、挂载时拉取一次不轮询。多账号用户不用再逐个切换查看余量。（#4887）
  OMO 模型选择器合并运行时模型
  OMO 表单的模型选择器此前只有静态列表；现在还会运行 opencode models，把本机安装的 OpenCode 实际报告的模型合并进来。这个辅助进程被刻意关进沙箱：禁用项目级配置发现、工作目录钉死在 OpenCode 配置目录——打开一个表单绝不会执行某个项目的 .opencode/ 插件；整个过程限时 20 秒，超时后杀掉完整进程树（macOS / Linux 用进程组，Windows 用 taskkill /T，WSL 在发行版内部用 timeout）。任何失败都回落到静态列表并弹提示。（#5522）
  Qwen3.8 Max 内置定价
  qwen3.8-max 按官方牌价播种：每百万 token 输入 $2 / 输出 $6，缓存读取 $0.25，缓存写入 $2.50（输入价的 125%，官方显式上下文缓存费率）。按「不存在才插入」播种，你自己改过的价格不受影响。（#6053）
  变更
  预设清单维护
  NekoCode 与 Unity2.ai 的合作伙伴预设从所有应用、README 与语言文件中移除；七牛预设的排序调整。
  修复
  Codex 用量在计数器交错时被多算（最高数倍）
  会话导入器此前从单一高水位线对累计值求差——一份文件里只有一个单调计数器时这是对的，但真实日志里存在两种交错形态：网关把同一份未变化的快照换着限额桶反复重播，以及两个真正不同的累计计数器交替出现。旧算法在这两种形态上都会把重播或交替当成新增量，现场文件实测有 6 到 8 倍虚高。
  现在导入器优先读取每个事件显式携带的「本轮用量」，并按完整 token 签名识别重播。去重刻意收窄：一份快照只与同一来源自己的上一份签名或紧邻的前一个 token 事件比对——不与其它来源的旧签名比对，因为合法的计数器重置会重现旧数值，扫全表会把它误吞。只有累计值的旧格式回退到单一全局基线，语义与从前一致。
  修复用近 1,900 份、共 1.7 GB 的真实会话文件回放验证：与独立重算的理想值偏差 0.001%，残余差异全部是旧算法把计数器重置截断吞掉、新算法合法救回的请求。历史数据刻意不改写——回正方法见「升级提醒」。（#3011、#3015）
  被丢弃的工具调用不再伪装成「本轮完成」
  第三方 Chat 网关偶尔会返回缺函数名的工具调用，Chat → Responses 转换层此前把它们静默丢弃、还照样报告本轮 completed——Codex 看到一个「成功但无事可做」的回合，就安静地结束了智能体循环，一个本可诊断的上游故障变成了无声停摆。
  现在，当一轮里的工具调用全部被丢弃、没有任何可用项时，流式路径发出 response.failed，非流式路径报转换错误；判定挂在 status == "completed" 上，finish_reason: length 的截断保持自己的 incomplete 语义不受影响。三个丢弃点都会记录结构化、不含内容的日志字段——是否有 call id、参数字节数、结束原因——#4341 这类问题终于可以从真实流量里诊断。含有效工具调用的回合、纯文本回合与截断回合行为不变。
  OMO 的配置写进了它不再读取的文件
  OMO 4.19.3 把配置统一进 ~/.omo/omo.jsonc（其次 omo.json），迁移时会把旧的按应用文件改名挪走。此后 CC Switch——只认识旧路径——一直在写一个不在 OMO 配置链上的文件，供应商切换看似成功、实则全部落空（#5945）。
  现在检测到统一配置文件就把 OpenCode 配置写进 OMO 的 "[opencode]" 分区——并且只写在这里，因为 OMO 对根层 schema 严格校验，任何未知根键都会让它丢弃整份文件。写入把文档当 JSON5 对待：注释、键序与行尾原样保留，内容没变化就不落盘；每次写出前还会把结果重新解析、与预期做语义比对——如果序列化会损坏文档，写入被拒绝、原文件原样保留，绝不落盘一份坏文件（已知触发场景见「升级提醒」）。同一改动还把所有受管应用在 Windows 上的原子写入换成 ReplaceFileW，堵上了旧「先删后改名」序列里目标文件短暂不存在的窗口。
  Copilot 接管在新版 Claude Code 上进不了登录态
  新版 Claude Code 对不认识的 API key 会弹确认框、默认选中「No (recommended)」——Copilot 接管此前写入的 ANTHROPIC_API_KEY 占位符正好撞上它：按推荐走，key 被忽略，用户面对的是一个未登录的会话。接管现在改写 ANTHROPIC_AUTH_TOKEN 占位符，零弹窗直接进入；在供应商表单高级区显式选择了 ANTHROPIC_API_KEY 字段的供应商保持旧行为。Copilot 转发路径还补上了其它路径早就有的 [1M] 上下文标记剥离，claude-*[1M] 形态的模型 ID 不再原样发给 GitHub 的 API。（#5832）
  Hermes 提示词写错了文件名
  Hermes 从 ~/.hermes/ 加载的身份文件是 SOUL.md，从不在那里找 AGENTS.md——后者是项目级上下文，从工作目录向上发现。CC Switch 的提示词管理从加入 Hermes 支持那天起写的就是 ~/.hermes/AGENTS.md，启用 Hermes 提示词产出的是一个死文件。现在读写 ~/.hermes/SOUL.md，既有的回填机制照常生效：你自己写过的 SOUL.md 会先导入数据库再被替换。（#5777）
  带同名空壳目录的 Skill 仓库装不上
  安装 ast-grep 官方 Skill 报「缺少 SKILL.md」：这个仓库在根下有一个与 Skill 同名的空壳目录，真正的 Skill 嵌套在更深处，而解析器返回了第一个名字匹配的目录。源目录解析现在以 SKILL.md 本身为锚点——没有它的目录绝不会被选中——顺带修好了同类仓库上「更新检查永远报有更新」的幻影提示。（#4141）
  skills.sh 嵌套 Skill 的 README 链接 404
  skills.sh 的发现流程只报告 Skill 的末级目录名；安装时能解析出真实的嵌套目录，但入库的 README 链接仍按目录名猜测构建，点开就是 404。链接现在从安装器实际解析出的目录生成。修复只作用于安装链路——旧版本写入的记录要重装一次才能恢复，见「升级提醒」。（#6111）
  全部应用启用时头部按钮被裁掉
  所有应用页签、项目切换器与接管开关同时显示时，头部溢出，「添加供应商」按钮被裁掉。主要操作现在固定在不收缩的区块里，应用切换器变成宽度感知：放不下的应用折叠进「更多」弹出层，当前应用始终可见。
  路由状态动画在后台空耗 GPU
  路由状态指示器的脉冲动画在窗口失焦时照常运行，为一个纯装饰效果持续占用 GPU。现在窗口焦点通过 data 属性与 CSS 控制心跳——失焦时动画冻结在完整不透明度，系统的「减少动态效果」偏好则完全禁用它。数据轮询不受影响，暂停的只是装饰。（#5767）
  安全加固
  无界读取全部封顶，确认框不再隐藏字段
  一轮六项的外部贡献加固（#5919）：
  - 用量脚本限资源限时：用量脚本可以经深链或同步的数据库到达本机，此前跑在没有任何限制的 JS 运行时上，一句 while(true) 就能把后端线程永远挂死。现在运行时强制 5 秒中断、16 MiB 内存与 256 KiB 栈上限。
  - Grok 会话日志读取设界：跳过超过 50 MiB 的文件，目录递归限深 16 层且不再跟随符号链接——~/.grok/sessions 下的一个符号链接环此前能造成栈溢出。
  - Codex 模型目录路径收紧：model_catalog_json 此前只看文件名就信任任意绝对路径；现在必须解析到 Codex 配置目录之内——canonicalize 之后复查一遍，符号链接逃不出去——单次读取上限 32 MiB。
  - 代理缓冲响应体封顶：会被完整收集的响应体——非流式响应、错误响应与需要整体校验的路径——上限 128 MiB，并且在分块到达时即时累计、越限立即断开连接，而不是先收完再比较；流式路径（透传与流式转换）本就不整体缓冲，不设总量上限。
  - 解压预算前置：解压配额加在解码器读取侧，gzip、deflate、zstd、brotli 全覆盖，压缩炸弹无法不受控地膨胀；越限响应映射为独立的 502，不会被误判为可重试的网络错误。
  - 深链确认框补上两个字段：供应商导入确认框此前会解析并持久化 usageAccessToken 与 usageUserId，却从不显示它们；现在两个字段都在批准前展示，令牌照常脱敏。
  性能
  备份：导出批量化，恢复单事务
  两个方向各有一个独立的成本。导出侧此前每行数据一条 INSERT，导入大备份时 SQLite 要逐条解析、准备、销毁数万条语句；现在按 200 行 / 1 MB 一批生成多行 INSERT，语句数少两个数量级，备份文件本身也缩小约 4 倍。恢复侧，每次 WebDAV / S3 同步导入后要把保留在本地的表逐行写回，此前每一行都是一个隐式事务——每行付一次完整的日志写入与 fsync，正是大库自动同步周期性卡住应用的元凶；现在整个恢复在单个事务内完成。
  旧格式的单行备份照常导入，新格式也在所有随应用分发的 SQLite 支持范围内，跨版本双向兼容。#2100 报告的跨机导入卡死很可能因此缓解——欢迎在该 issue 反馈实测结果。（#6122）
  Codex 用量全量重导入提速约 3 倍，Windows 收益更大
  全量重导入——导入 v16 之前的 SQL 备份、跨机恢复后游标不匹配、或手动重建时触发——在大语料上会把一个 CPU 核心跑满数分钟：每个 token 事件各自构成一个自动提交事务，每行付一次完整的日志创建 / fsync / 删除循环；每个归档文件还要跑一条用不上索引的游标继承查询。
  现在事件按 1,000 条一批提交，批间释放连接锁，界面查询得以插队；游标在与最后一批相同的事务里推进，崩溃永远不会让游标领先于数据；游标与模型定价每趟预载一次，热点语句预编译缓存。1,920 份 / 1.7 GB 的真实语料在 macOS（release 构建）上从 36.3 秒降到 11.1 秒；Windows 上每行 fsync 开销约数毫秒，绝对收益大一个数量级。改动前后回放同一语料验证等价：82,000 行导入结果在所有导出列上逐字节一致，导入 / 跳过计数完全相同。
  升级提醒
  本版没有数据库迁移
  v3.19.2 不含 schema 迁移（版本号保持 v16），不会触发升级前备份，升级即用。
  Codex 历史用量虚高需手动重建一次
  交错计数器修复只保证今后正确：历史行刻意不改写，也没有自动重建。如果你的看板里 Codex 数字高得离谱、且会话文件带交错形态，升级后到 用量页 → Codex 用量维护 → 「重建 Codex 用量」 手动重建一次即可——重建前会自动备份数据库；已删除的会话日志对应的历史无法重新导入。本版的导入提速让这次重建比以往快约 3 倍。
  大多数安装不受影响：不含交错形态的数据上，新旧算法的差异在千分之一以内。
  嵌套 Skill 的存量记录需要重装一次
  通过 skills.sh 安装、README 链接 404 的存量 Skill，卸载后重装即可恢复正确链接；原地「更新」不会重写它。
  升级后的首次同步会整体重传一次备份文件
  备份 dump 的格式变了，而同步协议把它当作整体哈希的不透明工件，所以升级后第一次 WebDAV / S3 同步会完整重传一次 db.sql——一次性、无害。
  Copilot 占位符在下次接管写入时生效
  AUTH_TOKEN 占位符在接管下一次重写 live 配置时生效——切换供应商或重启接管即可。在供应商表单高级区显式选过 ANTHROPIC_API_KEY 字段的供应商不受影响，你的选择原样保留。
  OMO 统一配置按文件存在性检测
  判定依据是文件是否存在、而不是 OMO 版本：~/.omo/omo.jsonc（或 omo.json）存在时就地编辑它；两者都不存在时照旧写传统的 OpenCode 层文件。已知限制：如果统一配置文件里含块注释（/* … */），写入会报错拒绝以保护文档——行注释（//）不受影响。在上游 JSON5 写入器修复之前，切换供应商前请先移除块注释。
  批量开关作用于整张列表
  管理面板的批量应用开关作用于全部条目，而不是搜索过滤后的子集。
  代理缓冲响应体上限 128 MiB
  需要完整缓冲的代理响应——非流式响应与错误响应——超过 128 MiB 时以 502 失败，不再转发；透传的流式响应不受影响。正常的 LLM 响应最多也只有几 MB，这个上限只会在上游异常时触发；这类失败会终止请求，不触发向下一个地址的故障转移。
  风险提示
  沿用的提示
  xAI Grok OAuth 登录：复用官方 Grok CLI 的公开 OAuth 客户端身份，使用可能导致账号被限制或封禁——详见 v3.18.0 release notes。
  Codex OAuth 反向代理：使用 ChatGPT 订阅的 Codex OAuth 反代可能违反 OpenAI 服务条款，详情见 v3.13.0 release notes。
  SuperGrok 配额查询：供应商卡片的配额展示依赖 grok.com 的非公开计费端点，xAI 调整接口后可能失效——详见 v3.19.0 release notes。
  第三方供应商路由：通过 CC Switch 本地代理把 Codex、Claude Desktop 或 Grok Build 的请求转换并转发到第三方供应商时，各供应商对计费、合规与数据留存的约束不同，请在使用前阅读目标供应商的服务条款。
  用户启用上述功能即表示自行承担相关风险。CC Switch 不对因使用这些功能而导致的任何账号限制、警告或服务暂停承担责任。
  致谢
  这一版 24 个提交里有 13 个 PR 来自外部贡献者——9 位贡献者，覆盖了从用量头条修复到安全加固的几乎每一条主线。
  代码贡献
  - #5854：Codex 交错计数器用量修复——本版头条，感谢 @MJYKIM99。作者自带真实文件证据、按评审意见三轮打磨到全量回放逐位收敛；也感谢 @ayanamislover 用自己的交错文件独立回放，与我们的验证互证。
  - #5919、#6122、#6119：六项安全加固、备份性能改造与 skills.sh README 修复，感谢 @zayokami——本版的「安全加固」与「性能」两个章节大半出自这三份工作。
  - #6011、#5522、#5767：OMO 统一配置适配、运行时模型发现与路由状态动画的 GPU 修复，感谢 @allenxu09。
  - #5967：管理面板搜索与批量开关，并顺手修掉数据层两个并发老问题，感谢 @YUZHEthefool——他还在 #6119 中协作把修复收敛到最小形态。
  - #4887：认证中心逐账号订阅用量，感谢 @SaladDay。
  - #5832：Copilot 接管兼容新版 Claude Code，感谢 @Suaig。
  - #5779：Hermes 提示词文件名修复，感谢 @mmm-05610——从报告 #5777 到提交修复一人完成。
  - #4153：Skill 源目录以 SKILL.md 为锚点解析，感谢 @makoMakoGo。
  - #6053：Qwen3.8 Max 内置定价，感谢 @mhy1227。
  问题反馈
  - 感谢 @KeShih 在 #3011 中精确指出「total_token_usage 非单调 / 重放被当作新增量」——本版头条修复的问题定义直接来自这条报告，他还在 #3015 中先行提出了修复思路。
ReleaseNotesUrl: https://github.com/farion1231/cc-switch/releases/tag/v3.19.2
ManifestType: defaultLocale
ManifestVersion: 1.12.0
