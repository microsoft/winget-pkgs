# Created by Anthelion using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: NextTrace.NextTrace.Core
PackageVersion: 1.6.4
PackageLocale: en-US
Publisher: NextTrace Community
PublisherUrl: https://github.com/nxtrace
PublisherSupportUrl: https://github.com/nxtrace/NTrace-core/issues
PackageName: NextTrace
PackageUrl: https://github.com/nxtrace/NTrace-core
License: GPL-3.0
LicenseUrl: https://github.com/nxtrace/NTrace-core/blob/HEAD/LICENSE
ShortDescription: NextTrace, an open source visual route tracking CLI tool
Description: An open source visual routing tool that pursues light weight, developed using Golang.
Tags:
- api
- as-path
- asn-lookup
- geoip
- geolocation
- ip-lookup
- network-analysis
- next-trace
- nexttrace
- traceroute
- utility
ReleaseNotes: |-
  What's Changed
  - 新增独立 CDN 测速模式 --speed：支持 Apple（默认）与 Cloudflare 后端、候选节点选择、指定 --endpoint、--source / --dev、TTY 输出、非交互模式和 --json 机器可读输出
  - 新增文本 IP 标注模式 --nali：可标注命令参数或管道输入中的 IPv4 / IPv6 字面量，并复用 NextTrace 现有 GeoIP provider、--data-provider、--language、--dot-server、--dn42、-4 / -6
  - MTR 的 Geo / RDNS metadata 改为异步补全：先显示基础 hop，再回填 Host / Geo 信息，减少 LeoMoeAPI、RDNS 慢查询对 TUI 启动和刷新的阻塞
  - 补强 LeoMoeAPI / WebSocket 链路：避免旧响应串入新请求、缺失 websocket 时解析异常、关闭竞态、失败 lookup 重试过密，以及 MTR API 信息缓存时间不刷新的问题
  - 修复测速模式的边界行为：--speed=false 不再误进入测速模式，测速 JSON 不再混入横幅文本，source binding 校验更严格，延迟/传输结果在降级场景下更稳健
  - 修复 --nali 的输入处理：取消 stdin 读取更可靠，非法 token 后会继续扫描后续 IP，缓存有上限，空标注不会被错误缓存
  - 恢复 Windows 上 --dev 的兼容行为：ICMP / TCP / UDP 都会从指定网卡解析 source IP 并继续探测，不再拒绝 TCP + --dev
  - 明确 Windows --dev 语义：它只通过 source address 影响路由选择，不代表真实绑定出接口；独立 --mtu 也遵循该 source-address 语义，并额外使用网卡名查询本地 MTU
  - 修复 WebUI deploy 监听竞态：服务端会先完成 bind 再输出访问地址，--listen :0 会显示实际监听端口
  - 改进 Windows / source-device 相关容错：WinDivert runtime 错误保留原始信息，不支持真实网卡绑定的平台不再把 SourceDevice 传入 ICMP 发送路径
  - 更新依赖：x/net、x/term、powclient v0.3.0，并将 release action 升级到 v3
  额外的：
  --speed 和 --nali 仅在完整版 nexttrace 中提供，nexttrace-tiny 与 ntr 不注册这两个参数。
  --speed --json 在 stdout 上只输出一个 JSON 文档；退出码 2 表示测速降级完成。
  Windows 上 --dev 不保证真实出接口；如果需要严格控制出口，请以系统路由表和指定 source IP 的实际行为为准。
ReleaseNotesUrl: https://github.com/nxtrace/NTrace-core/releases/tag/v1.6.4
ManifestType: defaultLocale
ManifestVersion: 1.12.0
