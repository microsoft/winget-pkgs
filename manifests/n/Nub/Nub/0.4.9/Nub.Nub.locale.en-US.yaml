# Created by Anthelion using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: Nub.Nub
PackageVersion: 0.4.9
PackageLocale: en-US
Publisher: nub contributors
PublisherUrl: https://nubjs.com/
PublisherSupportUrl: https://github.com/nubjs/nub/issues
Author: nub contributors
PackageName: Nub
PackageUrl: https://nubjs.com/
License: MIT
LicenseUrl: https://github.com/nubjs/nub/blob/HEAD/LICENSE
Copyright: Copyright (c) 2026 nub contributors
CopyrightUrl: https://github.com/nubjs/nub/blob/HEAD/LICENSE
ShortDescription: The all-in-one JavaScript toolkit that augments Node.js instead of trying to replace it.
Description: "An all-in-one toolkit for Node.js. One Rust binary to run your files and scripts, install dependencies, and manage Node itself — a Bun-like modern DX on top of the node you already have. There's no new runtime to adopt and no lock-in: every augmentation rides on Node's own public extension surfaces."
Moniker: nub
Tags:
- javascript-runtime
- node-version-manager
- nodejs
- package-manager
- script-runner
ReleaseNotes: |-
  Adds pnpm named-registry support, so dependencies that resolve through a registry alias install under nub.
  Named registries
  When pnpm is the incumbent package manager, nub now honors pnpm's namedRegistries — an alias-to-registry-URL map declared in pnpm-workspace.yaml or the global pnpm config.yaml. A dependency spec prefixed with the alias resolves from that registry:
  # pnpm-workspace.yaml
  namedRegistries:
    work: https://npm.example.com/
  // package.json
  { "dependencies": { "@work/constants": "work:1.x.x" } }
  Both direct and transitive named-registry specs resolve. The built-in gh: alias points at GitHub Packages (https://npm.pkg.github.com/), and a user entry can repoint it at a GitHub Enterprise host. Authentication is read from existing per-URL .npmrc entries — //host/:_authToken=… — so no new auth configuration is needed. (#444, #440)
  Before this, a named-registry spec fell through to the default registry and failed to resolve (no version of @work/constants matches range work:1.x.x).

  What's Changed
  - feat(resolver): support pnpm namedRegistries in pnpm-compat mode by @colinhacks in #444
ReleaseNotesUrl: https://github.com/nubjs/nub/releases/tag/v0.4.9
Documentations:
- DocumentLabel: Docs
  DocumentUrl: https://nubjs.com/docs
ManifestType: defaultLocale
ManifestVersion: 1.12.0
