# Created with winget-manifest scaffold for Nehoray.TranslationManager 1.1.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json

PackageIdentifier: Nehoray.TranslationManager
PackageVersion: 1.1.0
InstallerLocale: he-IL
Platform:
  - Windows.Desktop
MinimumOSVersion: 10.0.0.0
InstallerType: inno
Scope: machine
InstallModes:
  - interactive
  - silent
  - silentWithProgress
InstallerSwitches:
  # WinGet's default for InstallerType: inno is already
  # `/VERYSILENT /SUPPRESSMSGBOXES /NORESTART /SP-`; we set them
  # explicitly so a future schema default change can't surprise us.
  # /SP- skips the "This will install..." pre-install dialog.
  Silent: /VERYSILENT /SUPPRESSMSGBOXES /NORESTART /SP-
  SilentWithProgress: /SILENT /SUPPRESSMSGBOXES /NORESTART /SP-
  Custom: ""
UpgradeBehavior: install
# The "_is1" suffix is what Inno Setup appends to the AppId GUID when
# it writes its Uninstall registry key under
# HKLM\Software\Microsoft\Windows\CurrentVersion\Uninstall. WinGet uses
# this for upgrade detection and uninstall.
ProductCode: '{B0D4F2A7-3CCE-4A1A-9C44-7E1A1B6F0001}_is1'
ReleaseDate: 2026-05-25
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/nehorayc04/translation-launcher/releases/download/v1.1.0/TranslationManager-Setup-1.1.0.exe
    InstallerSha256: 5F13260588F24954F3913639C274E32F8997B251A108E6243A76741BE0380CFB
ManifestType: installer
ManifestVersion: 1.6.0
