# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json
# Created with winmatsch

PackageIdentifier: GDQuest.GDScript.Formatter
PackageVersion: 0.25.0
PackageLocale: en-US
Publisher: GDQuest
PublisherUrl: https://github.com/GDQuest
PublisherSupportUrl: https://github.com/GDQuest/GDScript-formatter/issues
PackageName: GDScript Formatter
PackageUrl: https://github.com/GDQuest/GDScript-formatter
License: MIT
LicenseUrl: https://github.com/GDQuest/GDScript-formatter/blob/main/LICENSE
Copyright: Copyright (c) 2025 GDQuest
CopyrightUrl: https://github.com/GDQuest/GDScript-formatter/blob/main/LICENSE
ShortDescription: A faster code formatter for GDScript and Godot 4
Description: |-
  A fast code formatter for Godot's GDScript programming language built with Tree Sitter GDScript and
  Topiary.

  The goal of this project is to provide a simple GDScript code formatter that runs fast enough and
  that's both easy to contribute to and to maintain while an official one built into the engine
  remains unavailable. Making it in user space allowed us to get a production-ready tool quickly and
  to update it anytime from here on.

  This project aims to follow the official GDScript style guide
Tags:
- formatter
- gdscript
- godot
- godot-engine
- rust
- tree-sitter
ReleaseNotes: |-
  A fast code formatter for GDScript in Godot 4.

  ## Changelog

  ### Added

  • Added `--verbose` option to print one line per formatted file (#227)

  ### Changed

  • Removed space between lambda function name and parameter list
  • Force @export and @onready annotations to stay on the same line as a variable, but keep other annotations separate, including annotations on local variables (#333)
  • Stop trying to format any code containing parse errors. Until now we tried to still format definitions around the code with errors, but this can lead to cases where the formatter produces invalid code
  • Add line wrapping for annotation arguments (#330)

  ### Fixed

  • Fixed an extra comma being inserted after a trailing comment in a lambda function argument (#304)
  • fixed certain export annotations being moved out of their respective groups (#308)
  • Preserve up to one blank line used to group elements in "containers" like enums
  • Fixed losing blank line between statements in a body if the previous statement has an inline comment (#320, thanks @Buitragox for the fix)
  • Fixed various edge cases with ignored directories：paths are now normalized before comparison
  • Fixed special get syntax with parentheses having an extra space (#314)
  • Output warnings to stderr when using reorder and safe mode together (#332)
  • Removed the space between `...` and variadic parameter names (#331)
  • Fixed incorrect indentation for match patterns using `and` or `or` (#328)

  Learn more about the formatter in the [GDScript Formatter documentation](https://www.gdquest.com/library/gdscript_formatter/)
ReleaseNotesUrl: https://github.com/GDQuest/GDScript-formatter/releases/tag/0.25.0
ManifestType: defaultLocale
ManifestVersion: 1.12.0
