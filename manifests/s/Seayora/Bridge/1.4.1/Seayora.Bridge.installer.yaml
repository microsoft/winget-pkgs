# Created with node backend/scripts/winget_manifest.cjs
# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.12.0.schema.json

PackageIdentifier: Seayora.Bridge
PackageVersion: '1.4.1'
InstallerType: zip
NestedInstallerType: portable
NestedInstallerFiles:
  - RelativeFilePath: seayora-bridge\seabridge.exe
    PortableCommandAlias: seabridge
# The bridge runs as a Windows service that the installer creates; nothing here
# writes to the registry, so there is nothing for winget to uninstall beyond the
# command it put on the PATH. `seabridge uninstall` removes the service.
#
# No InstallModes: a portable package is extracted and linked, so winget runs no
# installer that could have a mode, and declaring two that do nothing is the kind
# of thing a reviewer reads as carelessness.
UpgradeBehavior: install
Installers:
  - Architecture: neutral
    InstallerUrl: 'https://seayora.com/api/cameras/bridge/download/seayora-bridge-1.4.1.zip'
    InstallerSha256: EC758B3E729B72633904FA6F187D477C3F42176119184150FA7B12601147F6EA
ManifestType: installer
ManifestVersion: 1.12.0
