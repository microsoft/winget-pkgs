# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.locale.1.12.0.schema.json

PackageIdentifier: SakanaYuyu.DeepSeekHarnessDesktop
PackageVersion: 0.1.0-rc.5-0.5
PackageLocale: en-US
ShortDescription: A cross-platform Tauri desktop shell for the DeepSeek Harness dsh web interface.
Description: DeepSeek Harness Desktop is a cross-platform Tauri/WebView desktop shell for the DeepSeek Harness dsh web interface, with a custom title bar, system tray, signed automatic updates, and task-completion alerts. On first launch, it checks for Node.js, pnpm, and an existing ~/.dsh home, downloading missing runtime and production dependencies when needed.
Tags:
- ai
- deepseek
- deepseek-harness
- dsh
- large-language-model
- llm
ReleaseNotes: |-
  What's new
  - The splash is a compact transparent window: a borderless radial frost, the official fish mark, a white DeepSeek wordmark, and a progress bar, without window scrollbars.
  - On Windows, Host and CLI children stay silent when the parent has no visible console, so dsh / Node / pnpm no longer flash a black window during boot or plugin install.
  - Quit (tray Quit, a saved Exit close, or updater restart) stops the Host Node process tree. The next launch reaps a leftover host if the recorded image still matches.
  - If dsh web exits because a loader plugin fails, the shell disables that entry and retries the Host.
  - Minimize to tray still keeps the Host running.
  Windows installation closes a running desktop process before replacing files and refreshes an existing desktop shortcut with the versioned DeepSeek fish icon.
ReleaseNotesUrl: https://github.com/Sakana-yuyu/deepseek-harness-desktop/releases/tag/desktop-v0.1.0-rc.5-0.5
InstallationNotes: On first launch, the application checks for compatible Node.js, pnpm, and an existing ~/.dsh home; it downloads missing runtime and production dependencies when needed.
Documentations:
- DocumentLabel: Documentation
  DocumentUrl: https://github.com/Sakana-yuyu/deepseek-harness-desktop/blob/master/apps/desktop-tauri/README.en.md
ManifestType: locale
ManifestVersion: 1.12.0
