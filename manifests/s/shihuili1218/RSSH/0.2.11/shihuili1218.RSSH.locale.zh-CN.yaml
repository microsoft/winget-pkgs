# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.locale.1.12.0.schema.json

PackageIdentifier: shihuili1218.RSSH
PackageVersion: 0.2.11
PackageLocale: zh-CN
ShortDescription: 为 AI 运维而生的 SSH 客户端
Description: |-
  为 AI 运维而生的 SSH 客户端

  为什么选 RSSH
  🤖 AI 排障
  不是又一个聊天框。它读终端里真实发生的事，提议只读优先的命令，每条都标注副作用、要你点「执行」才会跑。payload 离机前过 shape 校验 + 本地脱敏——你的密钥、内网地址不会原样发出去。
  🎨 彩色命令块
  每条命令和它的输出自动成块、左侧按色分隔。上千行滚屏里，一眼找到上一条命令的输出在哪儿。纯本地渲染，零远端依赖、不在服务器上装任何 agent。
  ⌨️ 一处配置，处处可用
  rssh open prod 从任意终端直接拉起会话——CLI 与 GUI 共用同一个 SQLite 库。同一套主机和密钥，还能跑在手机和 JetBrains 工具窗口里。

  功能
  - SSH —— 密码、私钥、键盘交互、跳板机（ProxyJump）
  - 终端 —— xterm 仿真、10 000 行回滚、关键词高亮、搜索
  - SFTP —— 远程文件浏览、上传/下载
  - 端口转发 —— 本地和远程，命名配置，实时流量统计
  - 本地终端 —— 自动识别 zsh/bash/PowerShell
  - 会话录制 —— asciicast v2 格式，变速回放
  - Profile 与凭据 —— SQLite 存储，可从 ~/.ssh/config 导入
  - 安全与同步 —— 密钥进系统钥匙串，按凭据控制同步范围，加密备份到你自己的 GitHub 仓库
  - 片段 —— 可复用命令快捷键（Cmd+E）
  - 移动端 —— 虚拟键盘栏（Ctrl/Alt/方向键/Tab/Esc）、安全区、栈式导航
  - IDE 插件 —— 在 JetBrains IDE 的工具窗口里运行 RSSH（共享数据目录）
Tags:
- sftp
- shell
- ssh
- 上传
- 下载
- 传输
- 控制台
- 文件
- 服务器
- 终端
- 远程
ManifestType: locale
ManifestVersion: 1.12.0
