# winget-pkgs version manifest for OpenASR Desktop.
#
# This directory is STAGING material, not a winget-pkgs submodule/checkout:
# these three files are exactly what would be copied into
# microsoft/winget-pkgs at manifests/c/Caelum/OpenASRDesktop/<version>/ for a
# PR. See SUBMITTING.md in this directory for the actual submission flow.
#
# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.12.0.schema.json

PackageIdentifier: Caelum.OpenASRDesktop
PackageVersion: 0.1.8
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.12.0
