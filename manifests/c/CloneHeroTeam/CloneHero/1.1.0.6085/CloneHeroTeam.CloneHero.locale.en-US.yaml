# Created with Devicie using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: CloneHeroTeam.CloneHero
PackageVersion: 1.1.0.6085
PackageLocale: en-US
Publisher: Clone Hero Team
PublisherUrl: https://clonehero.net/
PackageName: Clone Hero
PackageUrl: https://clonehero.net/
License: Proprietary
LicenseUrl: https://clonehero.net/eula
Copyright: © Clone Hero
ShortDescription: Clone Hero is a classic instrument based rhythm game for Windows, Mac, Linux, and Android
Description: Clone Hero is a classic instrument based rhythm game for Windows, Mac, Linux, and Android. It's playable with any 5 or 6 fret guitar controller, any midi drum kit, any game controller and even your keyboard! Jam out with Drums, 5-fret Guitar, or 6-fret Guitar online or local!
Tags:
- drums
- games
- guitar
- rhythm
- videogame
ReleaseNotes: |-
  - Improved server stability.
  - Fixed rescanning custom content.
  - Added a message when no servers are available.
  - Fixed a crash while loading phrases.
  - Fixed album art transparency.
  - Fixed scoring logic issues.
  - Fixed a crash when the streaming assets folder is missing.
  - Fixed missing font characters in some cases.
  - Fixed practice HUD offset handling.
  - Fixed crashes on some MIDI files.
  - Fixed the drum action menu not appearing on the score screen in 5-lane mode.
  - Reduced online song comparison message frequency.
  - Fixed stuttering when whammy SFX are enabled.
  - Improved main menu background quality.
  - Added audio stall detection and correction.
  - Upgraded Unity to 2022.3.11f1.
  - Fixed several online song validation issues.
  - Fixed Control Mapper trigger handling.
  - Improved memory usage by recycling audio sample channels and reducing SFX channel limits.
  - Fixed server maxConnections handling.
  - Improved audio system robustness and logging.
  - Fixed main menu background switching.
  - Simplified sustain scoring calculations for better stability.
  - Fixed dropless sustain scoring.
  - Fixed 6-fret lefty-flip rendering.
  - Fixed mixed-case filesystem song loading issues.
  - Fixed song list hitching caused by loading previews while scrolling.
  - Fixed exceptions with custom content thumbnails.
  - Improved game server logging.
  - Upgraded the server runtime to .NET 7.
  - Fixed audio desync when pausing in certain cases.
  - Reworked audio fadeout management for better robustness.
  - Fixed several memory leak cases.
  - Added the .sng single-file song format.
  - Updated credits.
  - Added toggleable practice-mode lyrics.
  - Added [coda] event support for .chart files.
  - Fixed profile creation inserting the activation key into the profile name.
  - Implemented multiplayer stem muting with volume and panning feedback.
  - Fixed Versus results showing default winner text in some cases.
  - Fixed menu song infinite loops when no songs can be loaded.
  - Fixed menu song selection after returning from the song list.
  - Fixed song previews not resuming after returning from the song list.
  - Fixed scrolling in Song Path Manager.
  - Removed Unity Gaming Services.
  - Removed Unity Analytics.
  - Fixed video background sync when unpausing.
  - Fixed an Android filesystem access crash.
  - Fixed clipped scrolling text.
  - Fixed blurry text at lower resolutions.
  - Fixed Ion drumkit hit detection issues.
  - Updated icons.
  - Updated translations.
  - Improved song list sorting and filtering performance.
  - Fixed missed ghosting cases in no-ghosting logic.
  - Improved built-in song audio quality.
  - Fixed Control Mapper button highlighting and UX issues.
  - Fixed song scanning when folders exist inside song folders.
  - Added a default anti-spam system with fret ghost limits.
  - Added a Spam Meter Mode anti-spam variant.
  - Implemented lift strumming for gamepad mode.
  - Added lift-to-open-note behavior for gamepad mode.
  - Improved broken chart handling, including chord snapping cases.
  - Added support for custom sustain cutoff thresholds in song metadata.
  - Fixed multiple drums parsing bugs.
  - Improved 6-fret visuals.
  - Changed 6-fret open notes to use wide sustains.
  - Added 6-fret sustain-held animation.
  - Improved 6-fret fret and note graphics.
  - Upgraded core game textures for higher-resolution presentation.
  - Fixed non-linear audio fades.
  - Fixed a crash when a drum lane has no charted notes.
  - Fixed a crash related to stem muting.
  - Fixed false-positive audio stall detection at the end of songs.
  - Fixed stall detection issues in practice mode.
  - Fixed practice-mode highway preview song offset handling.
  - Fixed background load order for global and song-specific media.
  - Fixed star power animation not working in builds.
  - Updated Discord Rich Presence to fix Linux issues.
  - Reduced lag spikes during star power activation animation.
  - Fixed co-op guitar intensity showing rhythm values.
  - Improved .sng parsing robustness.
  - Fixed audio and gameplay desync caused by rounding errors.
  - Changed pause handling to better support replay rewinds.
  - Reduced FPS counter allocations.
  - Fixed a sustain drop leniency exploit.
  - Enabled Unity graphics jobs.
  - Optimized MIDI chart parsing.
  - Optimized .chart parsing during scanning.
  - Optimized practice-mode song loading.
  - Disabled strumless HOPOs in gamepad mode.
  - Fixed loading songs with missing parts.
  - Added early whammy leniency.
  - Added pause-menu dropout for local multiplayer.
  - Fixed randomized features that were not behaving randomly enough.
  - Optimized logging performance.
  - Reworked open-note handling to support open chords.
  - Optimized note rendering overhead.
  - Added the sustain_cuttoff_threshold metadata flag.
  - Refactored MIDI and .chart parsing for better cross-format consistency.
  - Fixed several edge cases in 5-lane to 4-lane pro conversion.
  - Optimized song scanning dialog performance.
  - Changed metadata loading to merge .chart and .ini data.
  - Replaced the .ini parser with a more efficient implementation.
  - Improved main-thread and audio-thread signaling performance.
  - Fixed coda section parsing in MIDI files.
  - Added automatic handling for overlapping chart events and duplicate same-lane notes.
  - Changed drum star power activation generation to use ticks instead of time thresholds.
  - Fixed duplicate section handling on the same tick.
  - Changed highway fade rendering to a smoother screen-space implementation.
  - Improved .chart parsing resilience against unknown event discriminators.
  - Improved MIDI parser handling for tracks without notes or events.
  - Reduced chart parsing allocations through object pooling.
  - Fixed natural HOPO handling after open notes.
  - Fixed drum event parsing aborting remaining event types.
  - Improved Discord Rich Presence reconnection handling.
  - Changed flam notes to apply across all difficulties.
  - Added difficulty support to MIDI sysex event parsing.
  - Reimplemented star power gain and drain logic for deterministic behavior.
  - Added the chord_snap_threshold metadata flag.
  - Improved song list export performance.
  - Reworked song offset handling to support negative time offsets.
  - Removed the need for the end_events metadata tag.
  - Reworked online song startup to reduce black screens.
  - Synced sustain drops between online players.
  - Fixed an exclusive fullscreen crash.
  - Rewrote lyrics rendering to reduce frame spikes.
  - Added %f support to currentsong.txt output.
  - Added a color shuffle color-profile option.
  - Added scorestats.json export at the end of songs.
  - Added emoji fallback font support.
  - Fixed online drum metadata sync issues.
  - Enabled mipmaps for highways.
  - Reworked highway sprites to dynamically match highway rotation.
  - Changed hit detection to consistently use current song time.
  - Added a start screen that keeps menus hidden until a player joins.
  - Improved profile input handling for IME support.
  - Fixed song length display for songs longer than 24 hours.
  - Added CLI replay hash validation.
  - Added replay file output to the Clone Hero/Replays folder.
  - Changed internal tick storage to use a long integer type.
  - Reworked guitar track visuals.
  - Updated icons and sources.
  - Fixed SongPathManager path removal for the first list entry.
  - Added replay input streaming to the backend server.
  - Disallowed gamepad mode with autostrum.
  - Reworked gamepad mode into a true modifier.
  - Added a classic gamepad modifier.
  - Fixed playlist sort issues across platforms.
  - Rejected songs with invalid resolution, tempo, or time signature values.
  - Fixed highway asset load order so song highways can override global video highways.
  - Added slower A/B seek controls in the pause menu.
  - Added support for changing practice speed on speedup charts.
  - Fixed current color-profile selection indicators.
  - Added fast scrolling after holding scroll input.
  - Added star power lightning color-profile options.
  - Optimized song list scrolling.
  - Enabled song-specific custom assets by default.
  - Cleared infinite frontend state after misses to prevent unintended open-tap hits.
  - Implemented a release-based ghost detection algorithm.
  - Changed spam detection so it breaks combo without blocking the next note.
  - Updated the ghost note counter to refresh immediately.
  - Enabled menu wrapping globally.
  - Changed SongLibrary name sorting to also sort by artist.
  - Added a 6-fret sustain particle effect.
  - Fixed song deletion from the menu.
  - Fixed song search to jump to the first result in the current sort and filter.
  - Added ghost miss SFX.
  - Delayed opening credits until song scanning finishes.
  - Added default two-ghost-per-note spam detection.
  - Fixed song scanning crashes caused by malformed color tags.
  - Fixed the Star Power bar blue sprite persisting after Star Power ends.
  - Fixed infinite screenshot capture from the auto-screenshot setting.
  - Fixed heartbeat animation on the pause menu.
  - Fixed default song folder creation on startup.
  - Improved player stats background blur.
  - Fixed lyric word colorization in practice-mode previews.
  - Fixed practice-mode lyric state when changing or restarting sections.
  - Fixed setlist length display wrapping after 24 hours.
  - Fixed offline song failures while No Fail is enabled.
  - Fixed encrypted song scanning crashes.
  - Fixed BASS initialization when loading from the main menu scene.
  - Fixed preview fadeout crashes on IL2CPP platforms.
  - Fixed file cache crashes that caused random scanning errors.
  - Fixed profile.ini corruption when deleting profiles out of order.
  - Fixed sorting by Stars Earned and Percentage without a selected profile.
  - Fixed MIDI issues introduced by library updates.
  - Fixed solo counter reset issues in practice mode.
  - Fixed practice-mode speed display above 100%.
  - Fixed aliased modifier handling and blocked submissions with zero notes hit.
  - Fixed HOPO desync caused by incorrect strum consumption.
  - Fixed multiplier desync and crashes when star power ends at song completion.
  - Fixed several star power desync cases.
ReleaseNotesUrl: https://github.com/clonehero-game/releases/releases/tag/v1.1.0.6085-final
ManifestType: defaultLocale
ManifestVersion: 1.12.0
