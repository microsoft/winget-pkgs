# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: Corsinvest.cv4pve.report
PackageVersion: 1.5.0
PackageLocale: en-US
Publisher: Corsinvest Srl
PublisherUrl: https://www.corsinvest.it/
PublisherSupportUrl: https://www.corsinvest.it/cv4pve
PrivacyUrl: https://www.corsinvest.it/privacy
Author: Corsinvest Srl
PackageName: cv4pve-report
PackageUrl: https://github.com/Corsinvest/cv4pve-report
License: GPL-3.0
LicenseUrl: https://github.com/Corsinvest/cv4pve-report/blob/HEAD/LICENSE.md
Copyright: Copyright (c) 2026 Corsinvest Srl
CopyrightUrl: https://github.com/Corsinvest/cv4pve-report/blob/master/LICENSE.md
ShortDescription: Report Tool for Proxmox VE
Description: |-
  cv4pve-report exports your entire Proxmox VE infrastructure to a single
  navigable Excel file — nodes, VMs, containers, storages, users, firewall
  rules, backup jobs, resource pools and more. Every row is a hyperlink to
  its detail sheet.

  Features:
  - Single .xlsx file with one sheet per node, VM and storage
  - Fully navigable — summary rows link to detail sheets
  - Cluster: users, API tokens, TFA, groups, roles, ACL, firewall, backup jobs, replication, SDN, pools
  - Nodes: services, network, disks, SMART, RRD metrics, APT updates, SSL certificates, tasks
  - VMs/CTs: config, network, disks, RRD metrics, backups, snapshots, firewall, QEMU agent info
  - Storages: content inventory, RRD metrics
  - Three profiles: Fast, Standard, Full
  - Works with a read-only API token, no root access required
Moniker: cv4pve-report
Tags:
- corsinvest
- excel
- infrastructure
- inventory
- proxmox
- proxmoxve
- report
- xlsx
ReleaseNotes: |-
  What's new
  - Response files — host, username and password can be saved in a config.rsp file and passed as @config.rsp, so you don't have to repeat credentials on every run
  - Generation stats in Summary — the Summary sheet now shows how many rows each section produced and how long it took, plus a total duration at the bottom
  - ApiTimeout setting — new option to set the HTTP timeout in seconds for slow or high-latency clusters; leave at 0 to use the default (100 s)
  - QemuAgentTimeout setting — new option to control how long to wait for the QEMU guest agent before giving up; default is 3 seconds
  - Firewall log now opt-in per section — Node.Detail.IncludeFirewallLog and Guest.Detail.IncludeFirewallLog let you enable firewall logs independently for nodes and VMs/CTs
  Fixes
  - Firewall log no longer crashes the report when the API returns no data
  - Sys.Modify permission added to the required permissions documentation (needed for APT repositories, updates and package versions)
  Performance
  - Nodes fetched in parallel — all nodes are now queried at the same time (status, version, subscription, DNS, time, network), the same way VMs and containers already worked
  - Node detail queries parallelized — for each node: services and SSL certificates, APT repositories/updates/versions, and directory/ZFS pools are now fetched simultaneously instead of one after another
  - Network data written once — node network interface rows are collected during node processing and written to the Network sheet in a single pass, instead of incrementally
  What's Changed
  - refactor: consolidate packaging metadata into single config file by @franklupo in #13
  - fix: handle null firewall logs and add Sys.Modify to required permissions by @franklupo in #14
  - feat: add IncludeFirewallLog flag to node and guest detail settings by @franklupo in #15
  - feat: parallelise all API calls and add performance tuning docs by @franklupo in #16
  Full Changelog: v1.4.0...v1.5.0
ReleaseNotesUrl: https://github.com/Corsinvest/cv4pve-report/releases/tag/v1.5.0
ManifestType: defaultLocale
ManifestVersion: 1.12.0
