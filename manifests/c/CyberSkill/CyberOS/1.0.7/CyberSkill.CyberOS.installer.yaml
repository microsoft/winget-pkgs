# TASK-APP-006: winget installer manifest. InstallerSha256 re-derived from the actual GitHub
# Release artifact by release-pkgmgr-pr.yml (AC #5). ManifestVersion bumped to 1.12.0 to match
# winget-pkgs' current PR template ("the 1.12 schema") - 2026-07-21, not independently re-verified
# against the full JSON schema since no Windows/winget CLI is available in this environment.
PackageIdentifier: CyberSkill.CyberOS
PackageVersion: 1.0.7
InstallerType: nullsoft   # matches the existing NSIS output confirmed in release.yml's desktop job
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/cyberskill-official/cyberos/releases/download/v1.0.7/CyberOS_1.0.7_x64-setup.exe
    InstallerSha256: 64844e332bda7fc7d2b62ec80be7353ccf7d11a7590a24c77c1d359ae1294c37   # computed per spec 6, not hand-typed
    InstallerSwitches:
      Silent: "/S"              # NSIS's standard silent flag; AC #8 requires confirming against
      SilentWithProgress: "/S"  # CyberOS's actual NSIS installer via a real test run (spec 9)
ManifestType: installer
ManifestVersion: 1.12.0
