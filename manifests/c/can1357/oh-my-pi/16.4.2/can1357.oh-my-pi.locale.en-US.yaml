# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: can1357.oh-my-pi
PackageVersion: 16.4.2
PackageLocale: en-US
Publisher: Can Bölük
PublisherUrl: https://can.ac/
PublisherSupportUrl: https://github.com/can1357/oh-my-pi/issues
Author: Can Bölük
PackageName: oh-my-pi
PackageUrl: https://omp.sh/
License: MIT
LicenseUrl: https://github.com/can1357/oh-my-pi/blob/HEAD/LICENSE
Copyright: |-
  Copyright (c) 2025 Mario Zechner
  Copyright (c) 2025-2026 Can Bölük
ShortDescription: ⌥ AI Coding agent for the terminal — hash-anchored edits, optimized tool harness, LSP, Python, browser, subagents, and more
Description: |-
  omp is a terminal-first coding agent that runs on your machine, talks to any provider, and treats sessions like git branches.
  omp (pronounced “oh-em-pi”, binary omp) is a fork of Mario Zechner’s Pi. It runs as a single Bun process, drives any model provider you have credentials for, and ships a flat tool surface the model uses to read code, run commands, edit files, drive a debugger, and spawn subagents that coordinate over an in-process IRC bus.
  Sessions persist as JSONL under ~/.omp/agent/sessions/. You resume, fork, branch, and share them. Settings, credentials, plugins, and caches all live under ~/.omp/agent/. Nothing leaves the machine unless you call a tool that does.
Tags:
- agent
- agentic
- ai
- chatbot
- code
- coding
- large-language-model
- llm
- programming
ReleaseNotes: |-
  @oh-my-pi/pi-agent-core
  Fixed
  - Fixed serialization of BigInt tool arguments to prevent data loss during remote compaction.
  @oh-my-pi/pi-ai
  Fixed
  - Fixed compatibility with xAI by automatically downgrading OpenAI-specific tool calls and image detail settings during message history replays.
  - Fixed a race condition in shared SQLite OAuth token refreshes by implementing durable credential ownership and compare-and-set persistence to prevent stale refresh failures.
  - Fixed OpenAI Codex requests to include the required version header for newly gated models.
  @oh-my-pi/pi-catalog
  Fixed
  - Fixed OpenAI Codex model discovery to include the Codex version header alongside the client_version query parameter.
  @oh-my-pi/pi-coding-agent
  Fixed
  - Fixed an issue where BigInt values in tool arguments failed to serialize during session compaction.
  - Resolved an issue where GPT-5.6 over-delegated tasks by refining task fan-out and concurrency policies in the system prompt.
  - Fixed a race condition in concurrent MCP OAuth token refreshes across processes, ensuring rotating refresh tokens are only refreshed once and preventing stale token errors from clearing valid credentials.
  @oh-my-pi/omp-stats
  Fixed
  - Fixed a crash during stats synchronization on legacy session entries that lack a cost breakdown by falling back to catalog pricing when available.
  @oh-my-pi/pi-utils
  Added
  - Added stringifyJson utility with support for BigInt serialization.
  What's Changed
  - fix(codex): send version header with Codex requests by @roboomp in https://github.com/can1357/oh-my-pi/pull/5108
  - fix(auth): serialize MCP OAuth refreshes by @roboomp in https://github.com/can1357/oh-my-pi/pull/5116
  - fix(ai): adapted xai responses replay shapes by @any-victor in https://github.com/can1357/oh-my-pi/pull/5109
  Full Changelog: https://github.com/can1357/oh-my-pi/compare/v16.4.1...v16.4.2
ReleaseNotesUrl: https://github.com/can1357/oh-my-pi/releases/tag/v16.4.2
Documentations:
- DocumentLabel: Docs
  DocumentUrl: https://omp.sh/docs
ManifestType: defaultLocale
ManifestVersion: 1.12.0
