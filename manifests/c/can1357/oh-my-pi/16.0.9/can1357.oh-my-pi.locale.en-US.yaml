# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: can1357.oh-my-pi
PackageVersion: 16.0.9
PackageLocale: en-US
Publisher: Can Bölük
PublisherUrl: https://can.ac/
PublisherSupportUrl: https://github.com/can1357/oh-my-pi/issues
Author: Can Bölük
PackageName: oh-my-pi
PackageUrl: https://omp.sh/
License: MIT
LicenseUrl: https://github.com/can1357/oh-my-pi/blob/HEAD/LICENSE
Copyright: |-
  Copyright (c) 2025 Mario Zechner
  Copyright (c) 2025-2026 Can Bölük
ShortDescription: ⌥ AI Coding agent for the terminal — hash-anchored edits, optimized tool harness, LSP, Python, browser, subagents, and more
Description: |-
  omp is a terminal-first coding agent that runs on your machine, talks to any provider, and treats sessions like git branches.
  omp (pronounced “oh-em-pi”, binary omp) is a fork of Mario Zechner’s Pi. It runs as a single Bun process, drives any model provider you have credentials for, and ships a flat tool surface the model uses to read code, run commands, edit files, drive a debugger, and spawn subagents that coordinate over an in-process IRC bus.
  Sessions persist as JSONL under ~/.omp/agent/sessions/. You resume, fork, branch, and share them. Settings, credentials, plugins, and caches all live under ~/.omp/agent/. Nothing leaves the machine unless you call a tool that does.
Tags:
- agent
- agentic
- ai
- chatbot
- code
- coding
- large-language-model
- llm
- programming
ReleaseNotes: |-
  @oh-my-pi/pi-ai
  Fixed
  - Fixed OAuth login replacing all other active accounts for the same provider, allowing multiple OAuth accounts to coexist concurrently.
  - Fixed legacy api_key credentials not being replaced/disabled atomically upon upgrading to OAuth login.
  - Fixed a logic issue where AuthStorage lost session-to-credential stickiness upon CLI restarts, causing cold-starts for server-side prompt cache (KV cache) and wasting tokens.
  - Fixed GitHub Copilot Responses requests rejecting image inputs that carry the detail: "original" hint with an HTTP 400 by degrading the hint to "auto" for hosts that do not support it; other hosts still preserve native-resolution frames (snapcompact). (#2822)
  @oh-my-pi/pi-catalog
  Fixed
  - Fixed GitHub Copilot's anthropic-messages proxy being misclassified as a non-signing reasoning endpoint (replayUnsignedThinking: true). It forwards to signature-enforcing Anthropic, so replaying a stripped/unsigned historical thinking block as signature: "" — most visibly an end_turn-bound checkpoint/branch-return turn whose signature the transform must strip — caused a 400 Invalid signature that corrupted the session and re-tripped on every full history re-send (e.g. after toggling MCP servers). Copilot now degrades such blocks to text like the official API. (#2851)
  - Added a supportsImageDetailOriginal compat flag that resolves to false for GitHub Copilot, whose Responses endpoint rejects the detail: "original" image hint with a 400, and true for every other host. (#2822)
  @oh-my-pi/pi-coding-agent
  Added
  - Added a --print-thoughts flag for single-shot print mode that includes sanitized thinking blocks in the text output; default print output stays the final answer only. When thinking is hidden by the hideThinkingBlock setting, --print-thoughts un-hides it for the print run so the flag is not a silent no-op (an explicit --hide-thinking still wins).
  Fixed
  - Fixed active /goal mode being paused by internal compaction and session-switch lifecycle aborts, and made those switches persist wall-clock goal usage without charging time spent in another session to a preserved goal.
  @oh-my-pi/pi-tui
  Fixed
  - Fixed bottom-anchored fullscreen overlays keeping their body rows but clipping off footer actions when terminal-height clamping is applied, restoring plan-mode approval options on short or stale-size terminals (#2957).
  What's Changed
  - fix(ai): degrade Copilot Responses image detail "original" to auto by @wolfiesch in https://github.com/can1357/oh-my-pi/pull/2902
  - fix(auth-storage): atomic legacy api_key cleanup within sqlite transaction during oauth login by @cagedbird043 in https://github.com/can1357/oh-my-pi/pull/2929
  - Add print-mode thinking output flag by @oldschoola in https://github.com/can1357/oh-my-pi/pull/2960
  - fix(tui): preserve plan approval actions when overlay is clamped by @roboomp in https://github.com/can1357/oh-my-pi/pull/2961
  - fix(auth): persist session-sticky credentials in SQLite cache across restarts by @cagedbird043 in https://github.com/can1357/oh-my-pi/pull/2972
  - fix(session): prevent goal mode pause during compaction/switch by @usr-bin-roygbiv in https://github.com/can1357/oh-my-pi/pull/2986
  New Contributors
  - @wolfiesch made their first contribution in https://github.com/can1357/oh-my-pi/pull/2902
  Full Changelog: https://github.com/can1357/oh-my-pi/compare/v16.0.8...v16.0.9
ReleaseNotesUrl: https://github.com/can1357/oh-my-pi/releases/tag/v16.0.9
Documentations:
- DocumentLabel: Docs
  DocumentUrl: https://omp.sh/docs
ManifestType: defaultLocale
ManifestVersion: 1.12.0
