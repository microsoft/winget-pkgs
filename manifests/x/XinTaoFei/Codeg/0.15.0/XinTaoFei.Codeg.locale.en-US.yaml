# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: XinTaoFei.Codeg
PackageVersion: 0.15.0
PackageLocale: en-US
Publisher: codeg
PublisherUrl: https://github.com/xintaofei
PublisherSupportUrl: https://github.com/xintaofei/codeg/issues
PackageName: codeg
PackageUrl: https://github.com/xintaofei/codeg
License: Apache-2.0
LicenseUrl: https://github.com/xintaofei/codeg/blob/HEAD/LICENSE
ShortDescription: Aggregate and browse AI coding agent sessions (Claude Code, Codex, Gemini CLI, etc.) in one place. Desktop app, self-hosted server, or Docker.
Description: Codeg (Code Generation) is an enterprise-grade multi-agent coding workspace. It unifies local AI coding agents (Claude Code, Codex CLI, OpenCode, Gemini CLI, OpenClaw, Cline, etc.) in a desktop app, standalone server, or Docker container — enabling remote development from any browser — with conversation aggregation, parallel git worktree development, MCP/Skills management, chat channel interactions (Telegram, Lark, iLink, etc.), and integrated Git/file/terminal workflows.
Tags:
- agent
- agentic
- ai
- chatbot
- claude-code
- code
- codex
- coding
- gemini-cli
- large-language-model
- llm
- programming
ReleaseNotes: |-
  - feat(sync): Desktop-hosted web clients now stay in real time with the desktop app. When you open a running conversation from another client, Codeg joins the existing agent stream instead of starting a separate one, and sidebars update as conversations are created, renamed, deleted, or enter review/running states.
  - feat(chat-channel): Message Channel events can now be delivered to Webhook URLs as structured JSON, with per-URL enable switches, a management dialog, and safer failure logging.
  - feat(chat-channel): Channels can notify you when an agent is waiting for permission. User-message notifications are also available as an opt-in event with bounded prompt previews, and remain disabled by default.
  - feat(file-tree): The file tree context menu can now copy paths for the workspace root, folders, and files.
  - fix(sync): Shared conversations now queue overlapping sends instead of dropping them, preserve send order across desktop and browser clients, and avoid duplicate user prompts when joining an in-progress turn.
  - fix(chat-channel): Event delivery now fails closed when event-filter settings cannot be trusted, Lark cards render untrusted fields as plain text, and failed settings loads no longer overwrite existing filters or webhooks.
  - fix(clipboard): Copy actions now work in non-secure web contexts, including components that call navigator.clipboard.writeText directly.
  - chore(format): Added Prettier ignore coverage and normalized formatting.
ReleaseNotesUrl: https://github.com/xintaofei/codeg/releases/tag/v0.15.0
ManifestType: defaultLocale
ManifestVersion: 1.12.0
