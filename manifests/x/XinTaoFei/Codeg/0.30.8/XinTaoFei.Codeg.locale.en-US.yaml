# Created with YamlCreate.ps1 Dumplings Mod
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: XinTaoFei.Codeg
PackageVersion: 0.30.8
PackageLocale: en-US
Publisher: codeg
PublisherUrl: https://github.com/xintaofei
PublisherSupportUrl: https://github.com/xintaofei/codeg/issues
PackageName: codeg
PackageUrl: https://github.com/xintaofei/codeg
License: Apache-2.0
LicenseUrl: https://github.com/xintaofei/codeg/blob/HEAD/LICENSE
ShortDescription: Aggregate and browse AI coding agent sessions (Claude Code, Codex, Gemini CLI, etc.) in one place. Desktop app, self-hosted server, or Docker.
Description: Codeg (Code Generation) is an enterprise-grade multi-agent coding workspace. It unifies local AI coding agents (Claude Code, Codex CLI, OpenCode, Gemini CLI, OpenClaw, Cline, etc.) in a desktop app, standalone server, or Docker container — enabling remote development from any browser — with conversation aggregation, parallel git worktree development, MCP/Skills management, chat channel interactions (Telegram, Lark, iLink, etc.), and integrated Git/file/terminal workflows.
Tags:
- agent
- agentic
- ai
- chatbot
- claude-code
- code
- codex
- coding
- gemini-cli
- large-language-model
- llm
- programming
ReleaseNotes: |-
  codeg:// is now a real system link — an outside tool can hand the desktop app a conversation and land you on that tab, cold start included.
  The close button stops guessing (ask, minimize to tray, or exit — your call), and an agent's temp files no longer pile up: every launch gets its own scratch directory that's cleaned when it ends.
  Cline connects with your own provider again, CodeBuddy's background workers report into the card that launched them, and eleven bundled agents move up.
  New
  - codeg://session/<id> opens that conversation from outside the app — on macOS, Windows and Linux, whether codeg is already running or starts cold. (#725, @changexbc)
  - The close button is now a preference — ask every time, minimize to tray, or exit; the prompt has a "remember my choice" box, and Settings → General keeps it editable afterwards. (#633 requested by @galact-byte, #741 @galact-byte)
  - Antigravity's settings panel can reclaim leftover temp space — scan the system temp folder for directories older versions stranded, review each path, and delete only what you confirm; anything still in use is skipped. (#735 reported by @chenjing-x)
  Improved
  - Updated bundled agents: Claude Code ACP 0.77.0 (Claude Code 2.1.270), Codex ACP 1.11.0, Kimi Code 0.43.1, CodeBuddy 2.151.0, Grok 1.0.30, Qoder 1.1.53, Cursor 2026.09.10, Hermes 0.21.3, OpenCode 1.18.31, OpenClaw 2026.9.4, Cline 3.0.62.
  - The model and reasoning-effort selectors now mark the agent's own recommendation — Codex and Claude Code each name their default, badged "recommended" in all ten languages.
  - Claude Code's selectors drop the ambiguous "default" row and no longer replay a saved pick the agent has stopped offering, so each turn records the model it actually ran on.
  - A permission request Claude marks as not-to-be-approved-by-a-stray-keystroke now highlights Reject instead of Allow.
  - An agent no longer leaks its temp directory on every connect — each launch unpacks into its own scratch directory that is removed when the agent exits, and anything a crash stranded is swept at the next start. (#735 reported by @chenjing-x)
  - Downloaded agent binaries now survive a restart or a system cleanup tool — the managed binary cache moved out of the system cache folder, and existing installs are migrated in place. (#736 reported by @tyonia)
  - Managed agents are found more reliably — codeg puts its own install directories first on PATH, recognizes ~/.opencode/bin, and reads both OpenCode plugin layouts (1.18.30 moved the package directory). (#736 reported by @tyonia)
  - DeepSeek's session modes, options and approval buttons now follow your language — deepseek-acp advertises all of them in Simplified Chinese only, so codeg re-labels them at render time; your own model names are left untouched. (#733 reported by @Mistic92)
  - A "failed to parse JSON" error now names where it came from and no longer quotes the raw payload back at you, so a credential can't ride out inside an error message.
  Fixed
  - Cline sessions connect with your own provider again — they failed with "Authentication required" whatever was configured, and the settings panel now writes where Cline 3.x actually reads, so a save after the first is no longer silently dropped.
  - Cline's "Auto-approve tools" chip actually toggles now. (#709 reported by @udagos)
  - A CodeBuddy background worker reports into the card that launched it — no more second bubble full of raw <task-notification> XML, and the card carries the worker's status, summary and its own tool calls.
  - A turn an agent ran on its own no longer leaves the session looking frozen — a pill above the thread pulls it in, instead of you having to reopen the conversation.
  - DeepSeek sessions written by deepseek-acp 0.9.0 open again — they rendered empty, or were silently cut off at a migration point, because the session log filename changed in that version. (#734 reported by @fanliang123)
  - An MCP call with a query argument keeps its own name instead of being shown as "WebSearch" — codegraph_explore is codegraph_explore again, while Codex's real web search is still recognized. (#710 reported by @udagos, #729 @dawNotPoi)
  - A /word only becomes a command badge when the agent actually advertises it — a paste, a restored draft, a quick message or a to-do brief no longer turn /etc into a command you never picked. (#727, @Adam-Dalloul)
  - A restored draft or a reopened queued message no longer comes back empty.
  - Cursor retries a transient agent failure again — the managed cursor-agent bundle ships without its retry policy, so codeg patches it back in during install. (#738, @ZY-Dragon03)
  - Closing the stash, push, project-boot or session-import window brings the workspace back when it was hidden to the tray, instead of leaving the app running with nothing on screen. (#680, @Adam-Dalloul)
  - The Open Folder shortcut no longer promises a new window — it opens the folder in the workspace you're already in, and all ten locales say so. (#682, @Adam-Dalloul)
  Thanks to @changexbc, @galact-byte, @Adam-Dalloul, @dawNotPoi and @ZY-Dragon03 for contributing to this release, and to @chenjing-x, @tyonia, @Mistic92, @fanliang123 and @udagos for the reports.
ReleaseNotesUrl: https://github.com/xintaofei/codeg/releases/tag/v0.30.8
ManifestType: defaultLocale
ManifestVersion: 1.12.0
