# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.12.0.schema.json
#
# Version manifest template. Substitute `4.1.0` for the
# stable SemVer (e.g. `4.1.0`) when preparing the per-version
# directory under
# `manifests/p/packetThrower/PortFinder/<version>/`. The
# `envsubst` step in the README handles this.

PackageIdentifier: packetThrower.PortFinder
PackageVersion: 4.1.0
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.12.0
