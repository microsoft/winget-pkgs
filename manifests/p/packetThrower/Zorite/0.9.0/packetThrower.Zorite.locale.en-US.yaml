# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: packetThrower.Zorite
PackageVersion: 0.9.0
PackageLocale: en-US
Publisher: packetThrower
PublisherUrl: https://github.com/packetThrower
PublisherSupportUrl: https://github.com/packetThrower/zorite/issues
Author: packetThrower
PackageName: Zorite
PackageUrl: https://github.com/packetThrower/zorite
License: GPL-3.0
LicenseUrl: https://github.com/packetThrower/zorite/blob/HEAD/LICENSE
Copyright: © 2026 packetThrower / Zorite contributors
CopyrightUrl: https://github.com/packetThrower/zorite/blob/main/LICENSE
ShortDescription: A local-first outliner and daily-journal note app (Logseq-style).
Description: |-
  Zorite is a cross-platform (macOS + Windows + Linux) local-first
  daily-journal and outliner note app. Logseq-style: an infinite-scroll
  journal of daily pages plus linked [[wiki-link]] pages, written in
  Markdown and stored in a local SQLite database.

  Embed and annotate PDFs, drop in images, and search across
  everything — no cloud, no account; your notes stay on your machine.
Moniker: zorite
Tags:
- journal
- knowledge-base
- local-first
- logseq
- markdown
- note-taking
- notes
- outliner
- pdf
ReleaseNotes: |-
  Highlights
  Find anything, anywhere: a find bar for the journal feed (and real find
  support while editing), rich-text copy for pasting into anything, line
  numbers when you want them — plus a set of math-editor and rendering fixes.
  Added
  - Find in the journal — ⌘F on the Journal opens a floating find bar
    (like the PDF viewer's): type to match across every loaded day,
    case-insensitively, with every match highlighted and the active one
    emphasized; Enter / ⇧Enter or ‹ › step between matches, scrolling the feed
    to each, and Esc closes. Works in both editing and reading views.
  - Find in page, while editing — the page find bar used to search only the
    reading view; it now highlights and scrolls in the live editor too, so
    ⌘F works the same whichever way you write.
  - Rich-text copy — Copy/Cut and the page menu's "Copy contents" now put
    formatted text on the clipboard alongside the raw markdown: paste into
    Word, Mail, or Docs and headings, bold, links, and lists arrive formatted,
    while terminals and code editors still receive plain markdown. When you
    want the literal syntax in a rich surface, the new "Copy as Markdown"
    (selection menu) and "Copy contents as Markdown" (page menu) copy the
    raw source only.
  - Line numbers — an optional gutter beside the editor on pages and
    journal days (Settings → Appearance): logical source lines (wrapped text
    counts once), sitting in the margin so your text never shifts, restarting
    per journal day. Off by default; the reading view stays clean.
  - Underscore emphasis — _italic_ and __bold__ now style while
    editing (they always rendered in the reading view), with CommonMark's
    word-boundary rules so snake_case_names and CONST_MAX_VALUES stay
    plain and the shruggie keeps both arms.
  Fixed
  - Math — the reading view now honors a formula's left/center/right
    alignment (it always centered); the symbol palette paints above following
    content instead of sliding under the next day's header, docks above the
    formula when the window bottom would clip it, and is draggable anywhere by
    its grip (drags used to freeze vertically); the matrix toolbar drags
    smoothly too.
  - The Settings Font dropdown no longer offers .ZedMono / .ZedSans —
    gpui-internal aliases that only render inside Zed and silently fell back
    to the default face here.
  - Dependency refresh: lopdf 0.44, oxidize-pdf 4 (with the manifest
    requirement actually bumped — plain builds masked a lockfile mismatch that
    broke Nix builds), RaTeX 0.1.13, heic-decoder.
  Infrastructure
  - The winget submission now fires automatically after a release — the
    release: published trigger never actually fired (GitHub suppresses
    events from the default workflow token), and every submission to date had
    been manual without anyone noticing. First automatic run: this release.
  What's Changed
  - docs: the 0.9.0 changelog + find-bar docs (4d85f86)
  - ci: fix dead release trigger — winget PR never auto-fired (d51598f)
  - docs(todo): sweep completed items into Completed (64ccd55)
  - feat(journal): find in the feed — a floating ⌘F bar, both view modes (da20094)
  - feat(clipboard): rich-text copy, with plain "Copy as Markdown" variants (37919e4)
  - fix(reader): math blocks respect the alignment marker (a519e03)
  - feat(math): draggable in-line palette + smooth grip drags (00ebe7b)
  - fix(math): floating panels paint above later content (0cdab8c)
  - refactor(settings): Line numbers card moves to Appearance (12b581d)
  - feat(journal): line-number gutter on the feed (b368fab)
  - docs(todo): line-number gutter shipped (06c9646)
  - refactor(pages): line-number gutter becomes a margin rail (05d6b7d)
  - feat(pages): optional line-number gutter (Settings → Markdown) (c460655)
  - feat(editor): underscore emphasis — italic / bold in WYSIWYG (35c9f62)
  - fix(settings): don't offer gpui's internal font aliases (c825cef)
  - docs(todo): optional line-number gutter for pages (afd893a)
  - docs(todo): rich-text (HTML flavor) clipboard copy (dba5fcc)
  - docs: note PDF export doesn't shape ligatures (5c933f9)
  - fix(deps): complete the oxidize-pdf 4 bump — Cargo.toml requirement (c6a8ffc)
  - chore(deps): Bump the cargo-minor group across 1 directory with 4 updates (f5fe52b)
  - chore(deps): Bump lopdf from 0.43.0 to 0.44.0 (6a9dae0)
  - chore(deps): Bump heic_decoder from 019b2fd to 0998ddc (e86446d)
  - chore(deps): Bump oxidize-pdf from 3.1.0 to 4.0.1 (92599ce)
  - chore(deps): Bump the actions-all group across 1 directory with 2 updates (4a929d5)
  - chore(deny): ignore RUSTSEC-2026-0206 (rustybuzz unmaintained, upstream) (9c764a5)
  Full Changelog: v0.8.0...v0.9.0
ReleaseNotesUrl: https://github.com/packetThrower/zorite/releases/tag/v0.9.0
Documentations:
- DocumentLabel: Project page
  DocumentUrl: https://github.com/packetThrower/zorite
ManifestType: defaultLocale
ManifestVersion: 1.12.0
