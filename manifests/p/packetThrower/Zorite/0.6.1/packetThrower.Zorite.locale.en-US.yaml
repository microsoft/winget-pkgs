# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: packetThrower.Zorite
PackageVersion: 0.6.1
PackageLocale: en-US
Publisher: packetThrower
PublisherUrl: https://github.com/packetThrower
PublisherSupportUrl: https://github.com/packetThrower/zorite/issues
Author: packetThrower
PackageName: Zorite
PackageUrl: https://github.com/packetThrower/zorite
License: GPL-3.0
LicenseUrl: https://github.com/packetThrower/zorite/blob/HEAD/LICENSE
Copyright: © 2026 packetThrower / Zorite contributors
CopyrightUrl: https://github.com/packetThrower/zorite/blob/main/LICENSE
ShortDescription: A local-first outliner and daily-journal note app (Logseq-style).
Description: |-
  Zorite is a cross-platform (macOS + Windows + Linux) local-first
  daily-journal and outliner note app. Logseq-style: an infinite-scroll
  journal of daily pages plus linked [[wiki-link]] pages, written in
  Markdown and stored in a local SQLite database.

  Embed and annotate PDFs, drop in images, and search across
  everything — no cloud, no account; your notes stay on your machine.
Moniker: zorite
Tags:
- journal
- knowledge-base
- local-first
- logseq
- markdown
- note-taking
- notes
- outliner
- pdf
ReleaseNotes: |-
  Highlights
  Properties polish from a day of real use, plus a caret that finally knows
  its size.
  Added
  - /property — the / menu can now add a property: it inserts a fresh
    row and opens the in-place property form on it, key field ready with the
    autocomplete listing every key already in use. Escape without naming a key
    and the row cleans itself up.
  Fixed
  - Properties — three papercuts in the in-place property editor: an empty
    value field (a template's blank key::) had no click target, so the caret
    couldn't enter it; clicking a field always seated the caret at the end
    instead of where you clicked (the rendered panel also always focused the
    first row, whichever row you clicked); and typing key:: when a page named
    key exists auto-linked the key into [[key]] before the property could
    form — auto-link-as-you-type no longer treats : as completing a word.
  - Caret height — on lines taller than their text (a bullet's breathing
    room, or a line grown to fit an inline formula) the caret spanned the whole
    line; it now matches the text height, vertically centered like the glyphs.
    Headings keep their proportionally taller carets.
  What's Changed
  - docs(changelog): the 0.6.1 section (28f9353)
  - fix(editor): caret matches the text height, not the grown row (85cac83)
  - feat(properties): /property inserts a row and opens the form (1ebc6ed)
  - fix(properties): clicks land where clicked; empty fields editable; keys can name pages (7258813)
  Full Changelog: v0.6.0...v0.6.1
ReleaseNotesUrl: https://github.com/packetThrower/zorite/releases/tag/v0.6.1
Documentations:
- DocumentLabel: Project page
  DocumentUrl: https://github.com/packetThrower/zorite
ManifestType: defaultLocale
ManifestVersion: 1.12.0
