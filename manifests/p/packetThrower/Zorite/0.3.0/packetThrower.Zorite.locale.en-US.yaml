# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: packetThrower.Zorite
PackageVersion: 0.3.0
PackageLocale: en-US
Publisher: packetThrower
PublisherUrl: https://github.com/packetThrower
PublisherSupportUrl: https://github.com/packetThrower/zorite/issues
Author: packetThrower
PackageName: Zorite
PackageUrl: https://github.com/packetThrower/zorite
License: GPL-3.0
LicenseUrl: https://github.com/packetThrower/zorite/blob/HEAD/LICENSE
Copyright: © 2026 packetThrower / Zorite contributors
CopyrightUrl: https://github.com/packetThrower/zorite/blob/main/LICENSE
ShortDescription: A local-first outliner and daily-journal note app (Logseq-style).
Description: |-
  Zorite is a cross-platform (macOS + Windows + Linux) local-first
  daily-journal and outliner note app. Logseq-style: an infinite-scroll
  journal of daily pages plus linked [[wiki-link]] pages, written in
  Markdown and stored in a local SQLite database.

  Embed and annotate PDFs, drop in images, and search across
  everything — no cloud, no account; your notes stay on your machine.
Moniker: zorite
Tags:
- journal
- knowledge-base
- local-first
- logseq
- markdown
- note-taking
- notes
- outliner
- pdf
ReleaseNotes: |-
  Highlights
  WYSIWYG table editing matures, inline-formatting shortcuts, and a Windows/Linux
  menu bar — plus inline HEIC/AVIF images.
  Added
  - HEIC, HEIF, and AVIF images render inline like JPEG and PNG, on macOS,
    Windows, and Linux.
  - Edit tables like a spreadsheet — arrow keys move cell-to-cell keeping your
    column (skipping the separator row), Enter drops to the cell below, and the
    caret enters and leaves the table cleanly at the top and bottom.
  - Table editing handles — hover a table for "+" strips to add a row or
    column and "−" handles to delete one; a right-click menu adds insert/delete,
    per-column alignment, and "Delete table".
  - Lists continue themselves — Enter in a list or task carries the marker to
    the next line; Enter on an empty item exits the list.
  - Inline formatting shortcuts — ⌘B / ⌘I / ⌘E (Ctrl on Windows/Linux) toggle
    bold, italic, and code around the selection.
  - Clickable task checkboxes — toggle ☐/☑ with a click; the cursor turns to a
    pointer over anything clickable.
  - Menu bar on Windows and Linux — a File / Edit / View menu now lives in the
    titlebar on those platforms (macOS keeps its native menu bar).
  Changed
  - Menus follow your theme — the table right-click menu and spell-check
    suggestions use the active theme's colors instead of a fixed dark style.
  Fixed
  - Adjacent tables stay separate — two tables with no blank line between them
    no longer merge into one grid.
  - The caret stays in view — arrowing up or down now scrolls the page so the
    caret never slips off-screen.
  What's Changed
  - docs(changelog): v0.3.0 (eac7aea)
  - feat(app): in-titlebar File/Edit/View menu for Windows/Linux (abf0a6a)
  - feat(editor): scroll the caret into view on vertical moves (49f787d)
  - fix(editor): split adjacent tables instead of merging them (ba98f5b)
  - feat(editor): delete a whole table from the right-click menu (2422172)
  - feat(editor): Enter moves to the cell below inside a table (3229308)
  - fix(editor): bottom gutter below tables so the add-row "+" can't be typed through (e680d16)
  - feat(editor): cell-aware table caret navigation (3bac8fd)
  - feat(editor): list auto-continuation + inline format toggles (fdec3e6)
  - feat(editor): theme popup menus + box-style add tabs (da36452)
  - feat(editor): table delete handles, gutters, right-click alignment (b6505a8)
  - fix(editor): half-moon table tabs + keep the caret put on add (d624b55)
  - feat(editor): hover-revealed table add-row/column "+" strips (a7ddd40)
  - feat(editor): clickable task checkboxes + cursor affordances (5de8c9f)
  - docs(changelog): note HEIC/HEIF/AVIF image rendering (#17) (e44c547)
  - feat(images): render HEIC/HEIF/AVIF via pure-Rust heic_decoder (#15) (7c98880)
  Full Changelog: v0.2.1...v0.3.0
ReleaseNotesUrl: https://github.com/packetThrower/zorite/releases/tag/v0.3.0
Documentations:
- DocumentLabel: Project page
  DocumentUrl: https://github.com/packetThrower/zorite
ManifestType: defaultLocale
ManifestVersion: 1.12.0
