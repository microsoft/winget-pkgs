# Created with WinGet Releaser using komac v2.16.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: packetThrower.Zorite
PackageVersion: 0.10.0
PackageLocale: en-US
Publisher: packetThrower
PublisherUrl: https://github.com/packetThrower
PublisherSupportUrl: https://github.com/packetThrower/zorite/issues
Author: packetThrower
PackageName: Zorite
PackageUrl: https://github.com/packetThrower/zorite
License: GPL-3.0
LicenseUrl: https://github.com/packetThrower/zorite/blob/HEAD/LICENSE
Copyright: © 2026 packetThrower / Zorite contributors
CopyrightUrl: https://github.com/packetThrower/zorite/blob/main/LICENSE
ShortDescription: A local-first outliner and daily-journal note app (Logseq-style).
Description: |-
  Zorite is a cross-platform (macOS + Windows + Linux) local-first
  daily-journal and outliner note app. Logseq-style: an infinite-scroll
  journal of daily pages plus linked [[wiki-link]] pages, written in
  Markdown and stored in a local SQLite database.

  Embed and annotate PDFs, drop in images, and search across
  everything — no cloud, no account; your notes stay on your machine.
Moniker: zorite
Tags:
- journal
- knowledge-base
- local-first
- logseq
- markdown
- note-taking
- notes
- outliner
- pdf
ReleaseNotes: |-
  Highlights
  The editing release: a Cditor-inspired WYSIWYG overhaul — markers hidden
  everywhere, Notion-style menus, a full table interaction suite — plus
  Logseq-style ((block references)) end to end, and a large rendering
  performance batch. The Cditor-inspired work is ported from
  @JYChen-8866's Cditor editor — thank you!
  Added
  - Block references, Logseq-style — type (( and pick any block from the
    palette; the reference shows the target block's text in both views, clicks
    jump to the block, and the picked block gets a stable anchor
    automatically. Notes keep portable Obsidian [[Page#^id]] links on disk
    while you read and edit the Logseq ((id)) form everywhere. A referenced
    block wears a small superscript count badge — click it for a list of every
    referencing page, each row jumping straight to the reference. Linked
    References cards render the referencing block as real markdown and jump to
    the exact line.
  - True WYSIWYG — formatting markers (**, _, ~~, backticks,
    <mark>) are hidden everywhere, not just away from the caret; styling is
    the feedback, the toggles edit it, and deleting across a hidden marker
    behaves like the marker isn't there. Emphasis nests (**bold *italic***)
    like the reading view.
  - Table suite — Cditor-style hover interaction (accent outlines, border
    pills to insert, red deletes), a table context menu (duplicate, visual
    styles, copy), drag-to-resize columns persisted into the markdown,
    double-click a column border to auto-fit it to content (Excel-style), and
    wide tables scroll horizontally in place — by trackpad, a mouse's side
    wheel, or dragging the slim scroll thumb — instead of squeezing columns.
    Ragged tables widen to their longest row like the reading view.
  - Notion-style chrome — the slash command palette gets categories and a
    flyout; context menus get icons, red destructive rows, and a selection
    format bar; tasks render as filled checkboxes with struck-through text;
    the journal feed and pages show scrollbars; code cards carry a Copy button
    and a language picker (both views).
  - Block drag-reorder — a six-dot grip in the gutter drags any line (and
    its indented children) to a new spot, with a drop indicator and a
    closed-hand cursor for the whole drag; "Turn into" in the right-click menu
    converts a block between paragraph, headings, lists, tasks, and callouts.
  - Whiteboard: mindmaps and flowcharts — adopted from ding-board: bound
    connectors that follow their boxes, plus mindmap and flowchart tooling.
  - Collapsible list headings — a heading inside a bullet or numbered item
    (- ### Notes) gets the fold chevron in both views; folding hides its
    indented children, outliner-style.
  - Appearance follows the OS — the default theme mode is now Auto.
  Fixed
  - Display math (#54) — a one-line $$…$$ renders as display math in
    both views, and prose sharing a line with $$ fences is split out at
    paste/typing/load time so formulas always get their own centered row.
  - Logseq importer: block refs (#53) — ((uuid)) references now import
    as real block links with anchors minted on their targets; title::
    overrides and journal-day targets resolve.
  - Properties render identically in both views — Logseq's props-under-a-
    block shape panels in the reader (tolerating CRLF and trailing spaces from
    real vaults), - key:: value props-only blocks panel in WYSIWYG, editing
    keeps each property's indent/list marker, and the reader's key column is
    measured like WYSIWYG's instead of fixed-width.
  - Linux — bold/italic render (the four core IBM Plex Sans faces are now
    bundled; gpui's Linux system font resolves to that family, which most
    distros don't ship), interactive window resize no longer stutters (the
    bounds save was a synchronous write per resize event), and the post-login
    blank beat is halved (the password KDF ran twice).
  - Windows — the drag-grip dots render evenly at fractional display
    scales, and Ctrl+Q no longer quits (Alt+F4 is the convention there;
    ⌘Q / Ctrl+Q stay on macOS and Linux).
  - Tables — selection highlights cover full rows without phantom
    stripes (wrapped cells included), column drags no longer reveal the
    style-marker line, GFM-escaped \| stays cell content, and right-clicking
    a selection inside a cell keeps it selected and offers Cut/Copy/Paste.
  - Editor correctness — CommonMark backtick runs and backslash escapes,
    CRLF normalized on paste, table-cell pastes stay in their cell, and the
    caret never loads parked on a hidden marker line.
  Performance
  - Windowed rendering — offscreen lines skip shaping and painting, and
    offscreen journal days render as fixed-height spacers, so huge notes and
    long feeds scroll smoothly in both views.
  - Cross-frame caches — per-line styled runs, table column widths and row
    wrap counts, and the content-structure scans are cached across frames;
    text shapes once per frame; IME offset mapping is O(distance).
  - Scroll anchoring — images and embeds finishing their async render
    above the viewport no longer jump the feed.
  What's Changed
  - docs(changelog): the 0.10.0 section (77fb0ac)
  - fix(linux): beta-2 bug batch — fonts, table interactions, resize, unlock (0fe067a)
  - feat(table): double-click a column border auto-fits it to content (e3c30fc)
  - fix(grip): drag-grip dots snap to the device pixel grid (ec92134)
  - fix(table): side-wheel scrolling works across the whole table (ccd931f)
  - feat(fold): headings inside list items are collapsible in both views (0f13740)
  - fix(properties): render Logseq property shapes identically in both views (4ab87ec)
  - feat(blockref): 100% ((id)) frontend + Logseq-style block backlinks (8656273)
  - chore(deps): Bump heic_decoder from 0998ddc to 2017f4d (4d5ffa9)
  - chore(deps): Bump the cargo-minor group with 4 updates (845fddf)
  - chore(deps): Bump the actions-all group with 3 updates (d742fcb)
  - feat(blockref): Logseq-style (( )) block-reference palette (6a80417)
  - feat(math): words never mix with display math — normalize, don't hide (#54) (c54eac6)
  - fix(math): one-line $$…$$ renders as display math in both views (#54) (838a7f3)
  - docs(todo): the v0.11.0 i18n plan (64b80a2)
  - fix(table): per-wrap-row selection bands inside a wrapped cell; CI lints (dddde0e)
  - perf(editor): per-frame/per-event efficiency batch — six review findings (e328f80)
  - fix(editor): CI clippy — range-loop indexing + placeholder arg count (fad4408)
  - refactor(editor): ShapedDoc struct replaces the 9-tuple ShapedLines (bb0c525)
  - refactor(editor): dedup batch — shared walks, table_left, grip formula (e5af317)
  - fix(editor): clippy needless_borrow — line_at already returns &str (76bb322)
  - feat(theme): default appearance is Auto (follow the OS) (ae6a8e5)
  - fix(editor): never park the loaded caret on a collapsed marker line (577f53d)
  - docs(todo): triage ultrareview PR #52 non-blocking findings (3cee980)
  - fix(editor): review blockers — occluded grip presses, turn-into grammar (3c050f6)
  - fix(editor): image and chip rows shape their hidden source unwrapped (9d0d0bd)
  - fix(table): selection highlight — full row bands, no phantom stripes (950b2bb)
  - fix(table): column drags no longer reveal the style-marker line (7672cf6)
  - feat(editor): wide tables scroll horizontally instead of scaling down (bacb2db)
  - fix(editor): keep the closed-hand cursor for the whole block drag (b1a57e6)
  - feat(editor): block drag-reorder via a gutter grip (39c3920)
  - perf(editor): windowed rendering — offscreen lines skip shaping and paint (7cf696f)
  - perf(journal): window reader days — offscreen days render as spacers (944d2e6)
  - fix(editor): ragged tables — grid widens to the longest row, short rows span (319764a)
  - feat(editor): "Turn into" block conversion flyout in the right-click menu (e258cc6)
  - perf(editor): cache table column widths and row wrap counts across frames (392cf69)
  - docs(todo): check off the cross-frame cache item (bcf8e92)
  - perf(editor): cross-frame per-line run cache (7b4c5e4)
  - docs(todo): check off scroll anchoring (b0adc79)
  - feat(editor): scroll anchoring — async renders above the viewport don't jump (5cd6530)
  - docs(todo): check off the scan-cache perf items (b2305d4)
  - perf(editor): cache the content-structure scans per content generation (faf93cf)
  - docs(todo): check off the double-shape memo + IME mapping items (0dcff8d)
  - perf(editor): shape once per frame; O(distance) IME offset mapping (31e0253)
  - docs(todo): check off nested inline emphasis (776b850)
  - feat(editor): nested inline styling — emphasis bodies re-scan recursively (2f43ae7)
  - fix(editor): hide escape backslashes like the reader consumes them (07f8fc7)
  - docs(todo): check off the audit bug batch (items 1-6) (cd23295)
  - fix(editor): CommonMark backtick runs + backslash escapes in the scanner (ba80a1d)
  - fix(editor): normalize CRLF on paste; keep table-cell pastes in the cell (f713783)
  - fix(table): treat GFM-escaped | as cell content, not a separator (52393b9)
  - docs(todo): Cditor audit findings — bugs, perf, and feature ports (b934b2e)
  - feat(whiteboard): adopt ding-board — mindmaps, flowcharts, bound connectors (1d1ab36)
  - feat(editor): Cditor-style deletion around hidden formatting markers (1a665b3)
  - feat(editor): formatting markers hidden everywhere — Cditor-style WYSIWYG (ce968f6)
  - docs(api): gpui-markdown table marker parsers + writer (1c402b9)
  - feat(table): drag-to-resize columns, persisted as markdown, both views (e6c0a42)
  - feat(table): Cditor-style hover interaction — accent outlines + border pills (41bfd81)
  - feat(table): Cditor-style table menu — duplicate, styles, copy, red deletes (200e503)
  - feat(menus): Cditor-style context menus — icons, red delete, format bar (657e5a4)
  - feat(slash): Notion-style command palette with a category flyout (613270d)
  - fix(code): keep hidden fences hidden — chrome polish, caret skips, quiet gutter (039f336)
  - fix(highlight): debug-only underflow panic mapping tokens to lines (68ba2dc)
  - feat(code): code-card chrome — Copy button + language picker (both views) (7e0f646)
ReleaseNotesUrl: https://github.com/packetThrower/zorite/releases/tag/v0.10.0
Documentations:
- DocumentLabel: Project page
  DocumentUrl: https://github.com/packetThrower/zorite
ManifestType: defaultLocale
ManifestVersion: 1.12.0
