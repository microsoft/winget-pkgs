# Created using wingetcreate 1.12.13.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.6.0.schema.json

PackageIdentifier: PowerPlatformProfessor.dvload
PackageVersion: 0.1.0
PackageLocale: en-US
Publisher: PowerPlatformProfessor
PublisherUrl: https://github.com/PowerPlatformProfessor
PublisherSupportUrl: https://github.com/PowerPlatformProfessor/dvload/issues
PackageName: dvload
PackageUrl: https://github.com/PowerPlatformProfessor/dvload
License: MIT
LicenseUrl: https://github.com/PowerPlatformProfessor/dvload/blob/main/LICENSE
ShortDescription: Map Excel and Power Query output to Microsoft Dataverse tables and load it via the OData Web API.
Description: |-
  dvload maps an Excel table — typically a Power Query output — to a Microsoft
  Dataverse entity and loads it through the OData Web API.

  The same engine backs three front ends: an Excel task pane, a browser UI
  (`dvload gui`), and the command line. Mappings are saved as portable
  .dvmap.json files, so a mapping built in either UI can be run unattended by
  the CLI on a schedule.

  Handles lookups (including customer and owner columns), upsert on
  alternate keys, choice columns, parent-then-child ordering, batching with
  configurable parallelism, and checkpoint/resume for interrupted runs.
  Sign-in is delegated by default and needs no Entra app registration;
  app-only auth with a secret or certificate is supported for scheduled runs.
Moniker: dvload
Tags:
- dataverse
- dynamics-365
- power-platform
- powerapps
- excel
- power-query
- odata
- etl
- data-import
ReleaseNotesUrl: https://github.com/PowerPlatformProfessor/dvload/releases
Documentations:
- DocumentLabel: Recipes
  DocumentUrl: https://github.com/PowerPlatformProfessor/dvload/blob/main/docs/RECIPES.md
ManifestType: defaultLocale
ManifestVersion: 1.6.0
