# Created using wingetcreate 1.12.13.0
# yaml-language-server: $schema=https://aka.ms/winget-manifest.defaultLocale.1.12.0.schema.json

PackageIdentifier: JagK.ClipboardTransformer
PackageVersion: 0.1.6
PackageLocale: en-US
Publisher: jag-k
PublisherUrl: https://github.com/jag-k
PublisherSupportUrl: https://github.com/jag-k/clipboard-transformer/issues
PackageName: Clipboard Transformer
PackageUrl: https://github.com/jag-k/clipboard-transformer
License: MPL-2.0
LicenseUrl: https://github.com/jag-k/clipboard-transformer/blob/v0.1.6/LICENSE
Copyright: Copyright (c) 2026 jag-k
ShortDescription: Rule-based clipboard transformer with a tray app and command-line interface
Description: Clipboard Transformer is a native tray application and standalone command-line interface that applies configurable rules to clipboard content.
Moniker: clipboard-transformer
Tags:
- automation
- clipboard
- productivity
- text
- transformer
- utility
ReleaseNotes: |-
  ### Added

  - **Rule groups:** (#29) Add `groups` and `group_imports` to the configuration, a
    versioned `<state_dir>/groups.json` state document, tray switches for
    rule-used groups with rule counts and descriptions, and `groups list` /
    `enable` / `disable` CLI commands. Group membership is inherited from
    rulesets and import edges; ignored groups and explicit group state disable
    the rules that depend on them. Short-form import edges accept `groups` and
    `ignore_imported_groups` as sibling keys, and group state may be written for
    an id before any rule uses it.

  ### Changed

  - **Dependencies:** Update `clap`, `zbus`, `timeago`, `schemars`, `uuid`,
    `log`, `futures-util`, and `ureq`, and drop the runtime's unused
    `thiserror` dependency. Keep the plugin-author and runtime configuration
    schemas on draft-07 and byte-compatible across the Schemars migration.
  - **Packaging/Nix:** Refresh the pinned `cachix/install-nix-action` v31
    revision.

  ### Fixed

  - **Developer tooling:** Point `just gen-config-schema` at the current
    `config schema` CLI command.
  - **CI:** Keep tray icon code generation clean under the Rust 1.98 Clippy
    slice-chunking lints.
  - **Release:** Keep `cargo-release` from pushing the release commit and tag
    together so required main-branch checks can finish before publication.
  - **Linux/Tray:** Detect Flatpak once in the runtime host and pass the sandbox
    state into the tray backend instead of sampling the environment twice.
ReleaseNotesUrl: https://github.com/jag-k/clipboard-transformer/releases/tag/v0.1.6
InstallationNotes: This per-machine MSI installs the tray application and standalone CLI and requires administrator approval. Open a new terminal after installation before running clipboard-transformer from PATH. The Windows binaries are not Authenticode-signed yet, so Windows SmartScreen or managed-device policy may display a warning.
Documentations:
- DocumentLabel: Installation
  DocumentUrl: https://github.com/jag-k/clipboard-transformer/blob/v0.1.6/docs/install.md
- DocumentLabel: Configuration
  DocumentUrl: https://github.com/jag-k/clipboard-transformer/blob/v0.1.6/docs/configuration.md
ManifestType: defaultLocale
ManifestVersion: 1.12.0
